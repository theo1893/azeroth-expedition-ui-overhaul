# 2026-09-01

## 新增 BigWigs 竖行条插件（参考 EasyDBM）
- 新文件 `Plugins/VerticalBars.lua`，已加入 `BigWigs.toc`（紧跟 Plugins\Bars.lua 之后）。
- 逻辑移植自 D:\EasyDBM\EasyDBM.lua：订阅 `BigWigs_StartBar` / `BigWigs_StopBar` 事件，把所有计时条转成竖向图标时间轴。
  - 剩余 >10s 的图标驻留轨道顶部；≤10s 沿竖轨匀速下落；≤5s 图标放大 1.3x 并显示特效圈（1.12 无 SetRotation，用 SetAlpha 透明度脉冲模拟）；倒计时文字 >10s 白、10~5s 白→黄、<5s 黄→红。
  - 中线橙色标记：最近一个 ≤10s 的技能位置；maxIcons 默认 3，多出的图标 0.3*iconSize 重叠偏移。
  - 去重逻辑同 EasyDBM：同 key + 同时长(±0.1s)且未过期 → 跳过；同 key 不同时长 → 视为刷新重置 endTime。key 用条目 text。
- 1.12 适配要点：Lua 5.0（table.getn、无 #）；SetScript 用 `this`/`arg1`；无 C_Timer（anchor OnUpdate 驱动）；无 BackdropTemplate（普通 SetBackdrop）；无 SetGradientAlpha（WHITE 用 `Interface\Tooltips\UI-Tooltip-Background` + SetVertexColor）；位置保存 GetLeft/GetTop*EffectiveScale（同 Bars.lua）。
- 配置：BigWigs 选项面板（启用/测试/图标数量/图标大小/轨道高度/显示名称/复位位置），配置存 BigWigsDB profile（defaultDB）；斜杠命令 `/bwv`（开关）、`/bwv test`（三条测试条 15/25/40s）。
- 交互：锁定按钮悬停显示、点击切换锁定；左键拖动（轨道背景也可拖）；右键轨道切换背景显示；PLAYER_ENTERING_WORLD / PLAYER_REGEN_ENABLED 清空 timeline。
- luaparser 语法校验通过（游戏内实测待用户验证）。
- 游戏内报错修复：`VerticalBars.lua:212: ')' expected near '%'` —— 根因是 **Lua 5.0 没有二元取模运算符 `%`**（Lua 5.1 才加入），必须用 `math.mod(a,b)`。共修两处：`remain % 60`（第 212 行）和 `count % 2`（第 402 行）。已记入 ~/.workbuddy/MEMORY.md 长期陷阱清单。
- 游戏内报错修复 2：`VerticalBars.lua:609: attempt to call method 'EnableMouse' (a nil value)` —— 根因是 **1.12 的 Texture 没有 EnableMouse/鼠标事件**（正式服才有）。把轨道背景 pathBG 的鼠标交互改为叠加透明 Frame 热区 `trackHit`（OnMouseDown 拖动 + OnMouseUp 右键切背景），ApplyLayout 同步定位 trackHit，UpdateLockState 改对 trackHit 开关鼠标；显式 SetFrameLevel（trackHit=2、lockBtn=3）防止热区盖住锁定按钮。已记入 MEMORY.md。
