# QUEST.LOG.GUTTER 当前工作文件 — QL-A2 V3.2

## 元数据

- 模块：任务日志／冒险者公会任务卷宗
- 组件 ID：`QUEST.LOG.LIST.PAPER`、`QUEST.LOG.DETAIL.PAPER`、
  `QUEST.LOG.GUTTER.UNDERLAY`、`QUEST.LOG.GUTTER.LEFT_FOLD`、
  `QUEST.LOG.GUTTER.RIGHT_FOLD`、`QUEST.LOG.GUTTER.STITCH`、
  `QUEST.LOG.GUTTER.TOP`、`QUEST.LOG.GUTTER.BOTTOM`
- 版本：`QL-A2 V3.2`
- 子状态：`repair-prepared`
- 项目阶段：`P3`
- 类型：`production`
- 执行状态：V3.2-A attempt 5 已通过目标级内审，等待批次重组复核；
  V3.2-B attempt 2 内审失败，attempt 3 已计数，固定执行器即将调用
- 本次授权：用户于 `2026-07-30` 明确回复“确认授权 QL-A2 V3.2-A/B
  及上述修复与上传范围”，授权 A／B 两段正文、各最多 `5` 次且最坏总计
  `10` 次调用、每次固定 SHA 的 Image 1／2 上传，以及同一目标前次输出仅
  在冻结合同内作为 edit 输入
- 固定执行器：`imagegen-0-143-0`／`@openai/codex@0.143.0`
- 操作：两个独立执行正文；V3.2-A 首次只生成两张纸页，V3.2-B 首次只生成
  六个页沟小件；旧版 QL-A2 失败候选永不上传
- 自动修复预算：
  - `V3.2-A`：最多 `5` 次固定执行器调用，含首次；当前 `5/5`，循环已停止
  - `V3.2-B`：最多 `5` 次固定执行器调用，含首次；当前 `3/5`
  - 最坏总调用数：`10`；任一正文提前通过即停止其循环，不消耗剩余次数
- 目标客户端：Turtle WoW `1.18.1`／Interface `11200`
- 对应合同：
  [SUBMODULES.md](../SUBMODULES.md)
- 锁定视觉基准／Image 1：
  [任务详情面板_视觉基准_v1.png](../../../../assets/locked/quests/任务详情面板_视觉基准_v1.png)
  — SHA-256
  `03dc589abad7187c478ec484cc6565f2c16d2ce52d2d6421251a4de6437453bd`；
  最高视觉权威，负责香草魔兽公会任务卷宗身份、综合色感、纸张与封皮
  关系、手绘轮廓、材料年代、低频正文纸面和装订的总体语义；
  忽略其中烘焙的任务行、文字、按钮、滚动条、徽记、蜡封和奖励框
- 基准提示词 provenance：
  [ART_BASELINE.md](../ART_BASELINE.md)
  — 与 Image 1 共同锁定物件隐喻、香草时代绘制语言、材料、配色、光照、
  磨损尺度和反现代约束
- 上级共用材质基线／不作为 V3.2 Image 输入：
  [聊天框独立艺术资源_v3.png](../../../../assets/locked/chat/聊天框独立艺术资源_v3.png)
  — SHA-256
  `272528e6d89cc90e5cbb37dce4ae572ddf9de0402078cdcf0ed5804f734faab8`；
  只通过书面基线继承纸张、旧皮革、手绘边缘、左上暖光和年代感的制作
  精度。V3.1 已证明把整张图作为视觉输入会把满页高频纹样带入纸面，因此
  V3.2 不上传此图
- 上级共用材质 provenance：
  [Chat 子模块美术基线](../../chat/SUBMODULE_ART_BASELINES.md)
  — 只用于解释共享材料与制作年代，不提供任务模块结构
- 结构次级参考／V3.2 Image 2：
  [QuestLogBookShell_Master_v1.png](../../../../assets/source/quests/ql-a1/QuestLogBookShell_Master_v1.png)
  — SHA-256
  `91f9fece41ed375df1fa32e94b18797cbb280c0b5e99478862473589c671edd5`；
  只负责已经接受的书体外轮廓、厚度、外围页叠、左上暖光与内部浅俯视角；
  不负责纸面微纹样、综合色感优先级、现有装订线或页沟造型
- 失败证据：本文件下方“尝试摘要”；只用于负面约束与 provenance，不作为
  Image 输入
- V3.2-A raw：
  `generated/quests/QL-A2/v3.2/pages/attempt-05/QL-A2_V3_2_A_PAGES.raw.png`
  — SHA-256
  `9de577d576719045467f3ef0ac33b64630bd0da63e66c77d38c1e549d6e0230f`
- V3.2-B raw：
  `generated/quests/QL-A2/v3.2/gutter/attempt-02/QL-A2_V3_2_B_GUTTER.raw.png`
  — SHA-256
  `eac0c962d8d1fce0d280e74c8776db290e45d0bf66464ceb808a2f164aed3bb5`
- 透明候选：
  `generated/quests/QL-A2/v3.2/pages/attempt-05/QL-A2_V3_2_A_PAGES.candidate.png`
  — SHA-256
  `47669c2d5c8243d47bc08cbe417be07bf74121a8a0e2aa8cae749ac109a106d8`
- 重组预演：未生成
- 最终 source：无

V3.1 正文、授权、执行与内部退回由提交 `1733678`／`52ff00f` 保存。V3.2
正文、完整性预检和修复边界已由本次授权冻结；首次调用前必须提交本文件。
本次执行只能写入被忽略的 `generated/quests/QL-A2/v3.2/`，不得修改
`assets/source/`，也不得导出 runtime 或接入 Lua。

## 尝试摘要

当前树只保留本组件这一份工作文件。曾执行 Prompt 的完整正文与 diff 由 Git
历史保存；以下摘要足以约束当前版本：

| 版本 | 结果 | 必须保留的结论 |
|---|---|---|
| V1 | 用户退回；会话 `019fac4a-c73e-71c1-a6bd-a94a86627b3e` | 外置封脊视角错误，上下端件压死翻页，图层／透视不一致；`42%／58%` 不能作为物理页宽，`140 × 60` 周期作废 |
| V2.1 | 用户退回；会话 `019fac8e-bae8-73f2-af89-674e925b0068`，结果 `ig_0bda33a80800f83f016a699ddd6dbc8191a674cb8b33717482` | 近等宽双页和内部页沟方向可保留；针脚偏离中心、端点与纸页融合差、正文高频纹理过密 |
| V3 | 内部退回；会话 `019fad38-517b-7ca1-82af-853b0ddc68f2`，结果 `ig_0e8d8bb14f37caff016a69c8e4f918819186e21dd3df807ff2` | Underlay 变成完整织纹竖条，内折变成实心纸条，正确层序下针脚消失，上下收口像把手，正文仍满页压花；流程还错误提升了派生 source 的权威 |
| V3.1 | 内部退回；授权提交 `1733678`；完整会话 `019faed6-8104-7ef2-94f7-8d80c5c885bc`，结果 `ig_07c0d476174c1236016a6a32eb8384819184676137de79b733`，raw SHA `47d14363049424b8c32b0eb486a87a0287adb4cc2af8f92e9416f0651cb796ea` | 八组数量成立，但正文仍满页压花；内折仍是双边纸条；针脚是粗长辫绳；线结完全外露；RGB 色键不均匀。不得透明化、重组或进入复审 |
| V3.2 | 当前 `repair-prepared / P3` | A attempt 5 已通过目标级内审；B attempts 1／2 均已退回，第 2 次同时违反格位、针脚和收口合同；attempt 3 已计数、即将调用；尚未进入用户复审 |

V3 raw SHA-256 为
`44e3cf1b01625b4c9e810229a6d33a9bcf381bb9bc0dc9feda06384034c0a0cc`；
透明候选 SHA-256 为
`97908ab5a32ee3b3ee37763d4b28dbb6dac4199a52c7424255805dc011271178`，
透明／半透明／不透明像素 `674973／32176／865715`。这些只证明技术处理，
不能恢复被否决的结构。

V3.1 raw 为 `1536 × 1024 RGB`，SHA-256
`47d14363049424b8c32b0eb486a87a0287adb4cc2af8f92e9416f0651cb796ea`。
检查器按 RGBA 读取时为 `1572864` 个不透明像素；精确 `#00FF00` 为 `0`，
启发式绿色主导像素为 `643188`。这些指标同时证明它没有满足真透明或均匀
纯绿色背景合同，但首个失败门禁仍是可直接观察到的组件语义／物理结构。

## 美术基准继承

### 权威顺序

1. Image 1 与
   [ART_BASELINE.md](../ART_BASELINE.md)
   共同构成最高视觉权威。
2. 已锁定的
   [任务模块基线](../ART_BASELINE.md) 与
   [全局基线](../../../GLOBAL_ART_BASELINE.md) 负责模块和跨模块约束。
3. [SUBMODULES.md](../SUBMODULES.md)
   对真实对象、数量、几何、层序、安全区、拉伸和禁止烘焙内容具有强制力，
   但不能改写前两项的美术 DNA。
4. Chat 锁定图与其书面基线只补充跨模块共同的材料制作精度；V3.2 不把
   该图上传给生成器，不能复制聊天框轮廓或纸面纹样。
5. Image 2 是已经接受的派生 source，只提供明确声明的结构职责，绝不高于
   Image 1，也不能决定正文微纹理或装订造型。
6. V1、V2、V2.1、V3 失败候选只提供负面证据，不作为任何 Image 输入。

### 必须继承的视觉 DNA

- 首先识别为一册正式、长期保存并反复翻阅的“冒险者公会任务卷宗”，不是
  两块普通羊皮纸、现代棕色面板、古董照片或暗黑祭坛。
- 保持 2004 年前后香草魔兽二维手绘位图语言：粗厚且略有手工误差的轮廓、
  明确明暗切面、略微夸张的实体厚度、温暖奇幻气质和低分辨率游戏美术感。
- 与战地旧书聊天框共享材料精度、笔触、左上暖光、磨损尺度和制作年代，但
  任务详情保持正式双页公会卷宗身份，不能复制聊天框的单页旧书轮廓。
- 综合色彩低饱和：暖赭与旧象牙纸面、烟褐与深胡桃阴影、暗酒红相邻封皮、
  少量氧化旧黄铜色反光。森林绿和褪色蓝灰只属于后续织物书签，本批不生成。
- 纸、麻线和衬布必须有不同的厚度、接触阴影与磨损方式；材料关系依靠手绘
  体积和遮挡表达，不能靠满页复古花纹或照片纹理表达。
- 主要阅读区平整、安静、低对比，装饰与高频磨损集中在不可拉伸边缘；保持
  可承载中文长正文的纸面，而不是均匀压花底板。
- 《上古卷轴 5》只提供沉重材料和低饱和配色，不继承极简文字菜单；禁止
  《暗黑破坏神 3》式尖刺黑铁、黑红高对比、骷髅与祭坛结构。

### 本批组件级转译

- 完整原型中的厚重感在本批由左右纸页的手绘厚边、内缘弯曲、凹陷页沟、
  半藏横向粗麻针脚和与外围书体一致的接触阴影表达。
- “正式公会卷宗”不通过徽记、蜡封或按钮烘焙表达，而通过同批纸张、克制
  装订、近等宽双页、较整齐但仍有手工误差的页缘表达。
- 正面游戏 UI 构图与书内视角统一为浅俯视：整体仍正对玩家，只有纸页表面、
  厚边、内折和页沟拥有一致的轻微深度，不允许单个部件变成侧视或独立 2D
  平板。
- 本批的纸张微纹理以 Image 1 的安静阅读区和原始提示词为准；Image 2 的
  满页细碎压花明确不继承。

### 明确不继承

- 不继承完整原型中的任务行、伪文字、页码、织物书签、黄铜书扣、包角、
  徽记、蜡封、滚动条、按钮、奖励槽和 `42%／58%` 的视觉列宽。
- 上述元素不是被放弃，而是分别属于后续 runtime 组件；本批不得把它们
  烘焙进纸面或页沟。
- 不继承聊天框的横向单页轮廓、索引签、输入条或页码结构。
- 不继承 Image 2 的现有页沟、针脚、满页压花和任何由先前生成造成的漂移。

### 冲突审计

- 原型提示词的左 `42%`／右 `58%` 指信息列；组件合同要求物理双页近 1:1。
  裁决：纸页外接尺寸相同，比例只在未来 runtime 文字布局中实现。
- 原型包含书签、页码、黄铜件和完整封皮；本批只有八个非交互内部结构对象。
  裁决：保留其材料年代和相邻色彩，不生成这些由其他组件持有的实体。
- 原型写“正面平视”，已接受结构 source 带轻微内部俯视。裁决：使用正对玩家
  的浅俯视 UI 视角；所有八对象共享同一表面法线、厚度尺度和左上光源。
- Image 2 的正文有高频压花，而锁定基准和原始提示词要求阅读区安静。
  裁决：Image 1 与其 prompt 优先，Image 2 纹样不继承。
- V3 曾把派生 QL-A1 source 称为最高权威。裁决：该权威倒置作废；V3.2
  两段执行正文必须明确 Image 1 最高、Image 2 仅为受限结构参考。
- V3.1 把大纸页与六个极小 gutter 件放在一张源画布，并上传聊天整框图；
  结果虽满足八组数量，却同时复发满页压花和小件形态退化。裁决：V3.2
  拆成 A／B 两次固定调用，聊天图只保留书面继承，不再作为上传输入。

## V3.1 失败后必须改变的生产结构

1. `GUTTER.UNDERLAY` 不再是完整的织纹竖条。它只能是大部分透明、无硬边、
   无可识别编织网格的凹陷阴影与低对比深胡桃衬布。
2. `LEFT_FOLD`／`RIGHT_FOLD` 不再是带四周完整轮廓和上下毛边的独立纸条。
   它们只能是大部分透明的单侧纸面弯曲渐变遮罩。
3. `GUTTER.STITCH` 必须在正确层序下仍露出中央短段；端点伸入内折遮罩，但
   不能被全部遮没，也不能出现两个深色端部结点。
4. `GUTTER.TOP`／`GUTTER.BOTTOM` 必须是紧凑、不规则、半藏的小线结，不能
   像横向把手、桥、U 形扣或两端带球的杆件。
5. 正文纸面不复制派生母版（本版 Image 2）中的满页均匀细碎
   压花。中央改为低频斑驳、轻纤维和缓慢色差；任何高频纹理只留在不可拉伸
   边缘。
6. 不再要求八种尺度挤在同一画布。V3.2-A 只负责两张大纸页；V3.2-B
   只负责六个 gutter 小件。两个调用共享 Image 1／Image 2、光源、综合色温
   和纸张批次，但分别接受独立对象数量门禁。
7. 聊天整框图不再作为 Image 输入；其年代、笔触和材料精度只从已经锁定的
   Chat 书面基线继承，避免生成器再次复制满页纹样。

## 组件合同

### 逻辑对象与数量

最终源画布必须恰好包含 `8` 组互不接触的对象：

1. `QUEST.LOG.LIST.PAPER`：左侧顶层纸页。
2. `QUEST.LOG.DETAIL.PAPER`：右侧顶层纸页。
3. `QUEST.LOG.GUTTER.UNDERLAY`：中央凹陷页沟底层。
4. `QUEST.LOG.GUTTER.LEFT_FOLD`：左页内折透明遮罩。
5. `QUEST.LOG.GUTTER.RIGHT_FOLD`：右页内折透明遮罩。
6. `QUEST.LOG.GUTTER.STITCH`：单个横向针脚站。
7. `QUEST.LOG.GUTTER.TOP`：顶部小型线结收口。
8. `QUEST.LOG.GUTTER.BOTTOM`：底部小型线结收口。

八组都是非交互普通状态。不得增加第九组装饰，也不得烘焙任何任务行、文字、
按钮、滚动条、奖励槽、书签或页码。

### 参考 runtime 几何

- `QuestLogFrame` 离线参考为 `676 × 440 UI px`，绝对中心线为局部
  `x=338`。
- 左右页源对象必须使用相同外接宽度与高度；允许纸边自然误差，但 `676 × 440`
  重组后的可见物理宽度差不得超过约 `1%`。
- `42%／58%` 只属于 runtime 文字列和控件占用，不得改变纸页外接尺寸、
  页沟位置或装订轴线。
- 页沟、针脚站、顶部收口和底部收口必须共享 `x=338`，重组中心偏移不得
  超过 `1 UI px`。

### 源画布与固定排布

- V3.2-A `PAGES` 目标画布：`1536 × 1024`，恰好两个对象。
  - 左页只占左侧 `768 × 1024` 隐形单元，右页只占右侧同尺寸单元。
  - 两张单层顶页使用相同可见外接宽高和至少 `32px` 连续裁切间距；
    左页专用内缘朝右，右页专用内缘朝左。
  - 不得生成外围多层页叠、封皮、页沟、针脚或任何第三组物件；外围页叠
    继续由已接受 QL-A1 shell 持有。
- V3.2-B `GUTTER` 目标画布：`1536 × 1024`，恰好六个对象，使用固定
  `3 × 2` 隐形网格，每格 `512 × 512`：
  - 上排从左到右：underlay、left fold、right fold；
  - 下排从左到右：单个横向 stitch、top closure、bottom closure。
  - 每个对象留在自己的单元中央，距单元边至少 `48px`；对象间不得接触、
    共享阴影或通过色键噪声连接。
- 两次调用都优先真正 RGBA 透明背景；若不能可靠透明，只允许完全均匀、
  无阴影、无渐变、无噪声的纯 `#00FF00` 色键。V3.1 的近绿色渐变背景不得
  复现。

### 每个对象的形态门禁

#### 左右纸页

- 两页同高、近等宽、同纸张批次，拥有相反方向的专用内缘曲面；每个对象
  只是一张单层顶页，不带外围多层页叠。
- 左页略暗暖赭，右页略亮旧象牙，但综合色差必须克制。
- 每页中央至少 `85%` 的阅读面积接近素纸：只允许低频云状斑驳、极轻纤维
  与缓慢综合色差。
- 禁止满页压花、浅浮雕字符、机械重复卷草、密集裂纹、网纹或均匀细碎噪声。
- 禁止在外侧与底部复制 QL-A1 的三至五层页叠；单层纸厚只通过短接触阴影
  和不可拉伸的毛边表达。
- 外侧毛边、页角、底边与专用内缘不可拉伸；中央安静区允许有限九宫格伸缩。

#### 页沟底层

- 以透明为主，只包含窄幅、向中央变深的柔和凹陷阴影，以及极低对比的深
  胡桃衬布色。
- 左右边缘必须长距离羽化到透明，不能拥有完整明边、硬直轮廓或矩形剪影。
- 不得出现可辨识的编织网格、辫绳、拉链齿、连续纵向绳杆、皮革竖带或外封脊。
- 不得像 V3.1 那样成为一根独立、等宽、从顶到底同样深的黑褐竖条。
- 中段允许纵向平铺，但平铺后不能产生重复花纹节奏。

#### 左右内折

- 每个对象大部分区域为透明，只保留一条单侧纸张高光、内弯综合色差和接触
  阴影。
- 左内折从左侧纸面向右侧页沟渐隐；右内折为正确镜像。
- 不得闭合成拥有四条完整边的纸条，不得有独立上边、下毛边、撕裂底边、
  外侧页叠或矩形填充。
- 单个源对象只能有一条可辨认的纸边；另一侧必须长距离羽化到透明，不能像
  V3.1 一样同时出现左右两条完整纸边。
- 中段允许纵向平铺；顶部、底部和横向宽度不可拉伸。

#### 横向针脚站

- 只是一段短小、中央略拱的横向粗麻线，左右端渐暗、渐细并伸入透明。
- 不能有深色圆头、独立绳结、孔环、皮革底板、矩形阴影或纵向绳段。
- 不得编成双股辫绳，不得投下独立绿色／黑色矩形阴影；可见长度不得超过
  所在 `512px` 单元宽度的 `35%`。
- 它必须在内折遮罩下隐藏两端，但中央仍可清楚辨认为横向针脚。
- runtime 通过离散放置重复；不得拉伸或纵向无缝平铺。

#### 顶部与底部收口

- 各自是一枚紧凑、不规则、略向页沟内凹的半藏麻线结。
- 两者使用相同线材尺度但不能机械复制。
- 外接宽度只略大于页沟，不得形成 U 形、横向把手、桥、横梁、杆件、
  蝴蝶结或两端带球的轮廓。
- 不允许拉伸、平铺或重复。
- 源图只能画出会在纸缘下露出的半结与短尾，不能像 V3.1 一样成为完整外露
  绳结。

### 固定层序

1. 已接受的 `QL-A1` 封皮与外围页叠。
2. `GUTTER.UNDERLAY`、离散 `GUTTER.STITCH`、`GUTTER.TOP`、
   `GUTTER.BOTTOM`。
3. 左右顶层纸页。
4. `GUTTER.LEFT_FOLD`、`GUTTER.RIGHT_FOLD`，只遮住针脚端部。
5. runtime 文字与控件。

### 验收预演与回退

用户复审前必须生成：

1. V3.2-A 两纸页 raw／透明候选与 V3.2-B 六 gutter raw／透明候选。
2. `676 × 440`、中心线 `x=338` 的真实层序重组。
3. 3、5、7 个针脚站的重组；三种情况下都必须看见离散横向针脚，且不得
   形成连续竖向构件。
4. 使用真实中文任务标题、长正文、目标和奖励文字的安全区预演。
5. 最小／基准／最大尺寸的纸面、页沟和内折拉伸检查。
6. 尺寸、模式、SHA-256、Alpha、绿色残留、对象边距和外接尺寸检查。

出现以下任一项，本次尝试立即退回：A 不是恰好两张单层纸页；B 不是恰好
六个指定小件；竖向织纹条、拉链或外封脊；独立实心内折纸条；针脚在重组后
消失；线结像把手；正文仍有满页高频压花；双页明显不等宽；文字或控件被
烘焙；背景不是透明或均匀纯绿。预算未耗尽且修正仍在下方不可变边界内时，
进入同一正文的自主修复；需要改变合同或新增输入时才创建新版本。不得用
Alpha 清理、后期平移或缩放掩盖结构错误。

## 生产正文完整性预检

- 复杂度：`assembly／repeat／stretch`。A 是双对象纸页源画布，B 是六对象
  固定图集，最终还要跨两张画布重组，因此采用最高详细度层级。
- 物件身份与范围：通过。A／B 正文分别写明准确对象名称、数量、顺序、
  非交互状态、动态内容排除和不得生成的相邻组件。
- 参考职责：通过。两段正文都逐张写明 Image 1 的最高视觉职责、Image 2
  的受限结构职责及两张图中必须忽略的内容；失败候选明确不作为输入。
- 构图与物理：通过。两段正文包含画布、单元／格位、边距、方向、浅俯视、
  同批尺度、左上光和关键对象关系；B 还逐件约束 underlay、fold、stitch
  与 closure 的物理身份。
- runtime 与安全区：通过。两段执行正文已写入安静正文区、不可拉伸边缘、
  离散针脚重复、端点遮挡意图与动态内容排除；`676 × 440` 重组、真实中文
  预演和最小／基准／最大拉伸属于确定性审查步骤，不是生成器输出，继续只
  保留在组件合同。
- 美术与技术：通过。香草时代笔触、材料层级、具体反模式、真透明／均匀
  `#00FF00` 以及最终对象级自检均在两段正文内，不依赖仓库链接才能理解。
- 未知但执行必需的值：无。最终 crop／UV 属于用户接受 source 后的
  `P4 → P5` 合同，不应在本次源资产生成前伪造。
- 去冗余结论：当前 V3.2-A／B 已达到可执行详细度，不追加泛化的“更厚重”
  “更史诗”等形容词。后续 `.rN` 仍须是完整自包含正文，不能只写与上一版
  的差异。
- 结论：`pass`。本次审计不改变 V3.2 的执行正文、修复边界或授权状态。

## 最终执行正文

### V3.2-A — PAGES

为 Turtle WoW 1.18.1 制作《魔兽世界》60 级香草时代视觉语言的二维手绘
位图组件源画布。本次只生成“冒险者公会任务卷宗”内部的两张单层顶页：
`QUEST.LOG.LIST.PAPER` 与 `QUEST.LOG.DETAIL.PAPER`。不要生成完整界面、
封皮、外围页叠、页沟、装订、按钮或任何第三个对象。

Image 1 是用户锁定的任务详情视觉基准，是最高视觉权威。严格继承它首先像
正式公会双页任务卷宗的物件身份、香草魔兽粗厚略不规则的二维手绘轮廓、
低饱和暖赭与旧象牙纸色、左上暖光、材料厚度和大面积安静阅读纸面；忽略
其中全部任务行、伪文字、标题、按钮、滚动条、徽记、蜡封、书签、奖励框、
黄铜件和封皮。

Image 2 是已经接受的 QL-A1 结构 source，只负责两页在同一本书中的浅俯视
方向、近等宽物理关系、相邻封皮综合色温和内外页缘朝向。不要复制 Image 2
的满页细碎压花、现有中央针脚、页沟、外围多层页叠、厚封皮或黄铜包角。
Image 2 不得覆盖 Image 1 的综合色感、安静纸面和绘制语言。不要使用任何
QL-A2 失败候选作为输入。

输出 `1536 × 1024`、无文字的组件源画布，恰好包含两个互不接触的对象。
左侧 `768 × 1024` 隐形单元只放左页，右侧同尺寸单元只放右页；两对象使用
相同可见外接宽高，四周保留至少 `32px` 连续裁切间距。左页专用内缘位于
右侧并向未来页沟自然弯曲，右页专用内缘位于左侧并形成正确镜像。两页共享
相同浅俯视表面方向、纸张批次、厚度尺度和左上光源。物理页宽近 1:1，绝不
通过拉宽右页表达 `42%／58%` 的文字列比例。

每个对象只是一张单层顶页，不是多层纸堆。外缘可有克制的手绘毛边、短接触
阴影和少量非镜像磨损，但不得在外侧或底部生成三至五层重复页叠。左页略暗
暖赭，右页略亮旧象牙，综合色差很小，仍明显属于同一批纸。

每页中央至少 `85%` 的阅读面积必须接近素纸，只允许大尺度、低频、边界模糊
的云状斑驳，极轻且不可重复的纸纤维，以及缓慢综合色差。中央不得出现任何
可辨认花纹。绝对禁止满页均匀压花、浅浮雕字符、符文状细纹、卷草、网纹、
密集裂纹、颗粒噪声、重复斑点或照片级古纸纹理。高频磨损只能集中在不可
拉伸的外缘、页角和专用内缘。

保持 2004 年前后香草魔兽手绘位图感：宽面笔触、明确但克制的明暗切面、
可见手工误差和温暖奇幻气质。禁止现代卡片、细金属线、透明黑板、照片级
古董、暗黑祭坛、聊天框单页轮廓，以及任何文字、任务行、图标、页码、
书签、按钮、滚动条、针脚、绳结、封皮或完整 UI。

背景优先真正 RGBA 透明；若无法可靠透明，只能使用完全均匀的纯
`#00FF00`。绿色背景上不得有渐变、噪声、纹理、光晕、环境阴影或投影。
最终自检：画面只有两张单层纸页；两页外接尺寸相同、内缘方向相反；中央
没有满页高频纹样；没有外围页叠或第三个对象；背景是真透明或均匀纯绿。

### V3.2-B — GUTTER

为 Turtle WoW 1.18.1 制作与同一册“冒险者公会任务卷宗”配套的二维手绘
页沟组件源画布。本次只生成六个独立非交互对象：
`GUTTER.UNDERLAY`、`GUTTER.LEFT_FOLD`、`GUTTER.RIGHT_FOLD`、
一个 `GUTTER.STITCH`、`GUTTER.TOP` 和 `GUTTER.BOTTOM`。不要生成纸页、
完整书本、封皮、按钮或任何第七个对象。

Image 1 是用户锁定的任务详情视觉基准，是最高视觉权威。继承它的正式公会
任务卷宗身份、香草魔兽宽面手绘笔触、低饱和暖赭／烟褐／深胡桃色、粗麻线
尺度、左上暖光和克制装订语义；忽略其中所有文字、任务行、按钮、滚动条、
徽记、蜡封、书签、奖励框和黄铜装饰。

Image 2 是已接受的 QL-A1 结构 source，只负责内部浅俯视方向、两页之间的
接触尺度、纸缘厚度和相邻综合色温。不要复制 Image 2 的现有中央针脚、
页沟、满页压花、外围页叠、封皮或包角。Image 2 不能决定本批六个对象的
形态。不要使用任何 QL-A2 失败候选作为输入。

输出 `1536 × 1024`、无文字的组件源画布，使用固定 `3 × 2` 隐形网格，
每格 `512 × 512`。上排从左到右恰好是 underlay、left fold、right fold；
下排从左到右恰好是单个 horizontal stitch、top closure、bottom closure。
每个对象留在自己单元中央，距单元边至少 `48px`；六组互不接触、不共享
阴影、不通过噪声相连。不要生成标签、分隔线、单元边框或第七组装饰。

上排左侧 underlay 大部分透明，只包含一条窄而柔和、向中央稍深的凹陷接触
阴影和极低对比深胡桃衬布色。它两侧长距离羽化到透明，纵向明暗有缓慢变化。
它没有完整硬边、矩形剪影、等宽黑褐竖条、编织网格、辫绳、拉链齿、皮革
竖带或外封脊，也不投下独立阴影。

上排中间 left fold 是左页内缘的一条单侧弯曲覆盖：只保留一条纸张高光、
轻微内弯综合色差和接触阴影，其余区域羽化透明。单个对象只能有一条可辨认
纸边，没有完整上边、下边、第二条纵向纸边、毛边底部、外侧页叠、封闭轮廓
或实心矩形。上排右侧 right fold 是它在物理方向上的正确镜像，同样只有
一条纸边，不能成为独立纸条。

下排左侧 stitch 只是一段短小、中央略拱的单股粗麻针脚。可见长度不得超过
所在单元宽度的 `35%`。左右端逐渐变细、变暗并消失到透明；没有双股编织、
辫绳纹理、圆头、孔环、绳结、底板、矩形阴影、投影或纵向绳段。多个副本
未来沿中心离散重复时，必须仍读作数个横向短针脚。

下排中间 top closure 与下排右侧 bottom closure 各是一枚紧凑、不规则、
略向页沟内凹的半藏麻线收口，只画出会从纸缘下露出的半结和短尾。两者使用
相同麻线尺度但不是机械复制。它们不是完整外露绳结、蝴蝶结、U 形扣、横向
把手、桥、横梁、杆件或两端带球的装饰。

六个对象共享同一浅俯视方向、左上暖光、低饱和综合色温、纸／麻线／衬布
厚度尺度和香草魔兽二维手绘精度。禁止照片级绳索、现代图标、黑铁结构、
霓虹、细金线、文字、纸页、任务行、按钮、滚动条、封皮或完整 UI。

背景优先真正 RGBA 透明；若无法可靠透明，只能使用完全均匀的纯
`#00FF00`。绿色背景上不得有渐变、噪声、纹理、光晕、环境阴影或投影。
最终自检：固定六格中各有且只有指定对象；underlay 不是完整深色竖条；
两个 fold 各只有一条纸边；stitch 是短单股针脚而非辫绳；两个 closure
只是半藏收口；没有纸页或第七个对象；背景是真透明或均匀纯绿。

### 固定执行映射

- V3.2-A 与 V3.2-B 的 `-i Image 1`：
  `D:\Git\azeroth-expedition-ui-overhaul\assets\locked\quests\任务详情面板_视觉基准_v1.png`
- V3.2-A 与 V3.2-B 的 `-i Image 2`：
  `D:\Git\azeroth-expedition-ui-overhaul\assets\source\quests\ql-a1\QuestLogBookShell_Master_v1.png`
- V3.2-A 第一次 raw／透明目标：
  - `generated/quests/QL-A2/v3.2/pages/attempt-01/QL-A2_V3_2_A_PAGES.raw.png`
  - `generated/quests/QL-A2/v3.2/pages/attempt-01/QL-A2_V3_2_A_PAGES.candidate.png`
- V3.2-B 第一次 raw／透明目标：
  - `generated/quests/QL-A2/v3.2/gutter/attempt-01/QL-A2_V3_2_B_GUTTER.raw.png`
  - `generated/quests/QL-A2/v3.2/gutter/attempt-01/QL-A2_V3_2_B_GUTTER.candidate.png`
- 第 `02`–`05` 次分别写入同级 `attempt-02`–`attempt-05`，文件名保持
  不变；不得覆盖任何前次 raw、透明候选或重组预演。
- Image 1／Image 2 只有在用户针对 `QL-A2 V3.2` 明确授权 A／B 两段正文
  与上述修复预算，并允许每次调用上传后，才能传给固定子进程；V3.1 授权、
  “继续”或对拆分方向的认可均不构成 V3.2 生成授权。

## 自主修复循环

### 共同不可变边界

- `V3.2-A` 与 `V3.2-B` 是两个独立授权正文，各最多调用固定执行器 `5`
  次，首次生成计为第 `1` 次；传输失败、正文截断、执行器错误或不可用输出
  同样占用一次。最坏总调用数为 `10`。
- 八个组件 ID、对象／状态数量、非交互性质、权威顺序、Image 1／2 的受限
  职责、两张 `1536 × 1024` 画布、固定单元／网格、runtime 几何、层序、
  Alpha／纯绿色键合同、禁止烘焙内容与验收预演全部冻结。
- Image 1 与 Image 2 是唯一外部图像输入；Chat 图、V1–V3.1 失败候选和
  A／B 另一目标的输出都不得作为输入。A 输出不能成为 B 的参考，B 输出也
  不能成为 A 的参考。
- 每次失败先记录第一失败门禁、可观察证据、应保留区域与 edit／regenerate
  决策，再把完整 `.rN` 正文写入本文件并提交；之后才可调用下一次。

### V3.2-A 可修复范围

- 不可变：恰好两张互不接触的单层顶页；左右 `768 × 1024` 单元；同外接
  宽高、近 1:1；相反内缘；至少 `85%` 安静阅读区；无外围页叠、页沟、
  针脚、第三对象或动态内容。
- 可自主调整：在上述合同内加强对象计数、构图留白、边缘处理、低频纸纹、
  色温和光照措辞。对象数量／身份、背景、满页压花、外围页叠或透视错误
  优先从锁定参考重新生成；仅当结构门禁全部通过而问题局限于边缘、纸色或
  局部纹理时，才允许把 A 的前次输出作为 A 的 edit 输入。

### V3.2-B 可修复范围

- 不可变：固定 `3 × 2` 网格中恰好六件；单元角色、顺序和 `48px` 边距；
  underlay、左右单侧 fold、短单股 stitch 与两个半藏 closure 的物件身份；
  无纸页、完整书本或第七对象。
- 可自主调整：在上述合同内加强单侧边、羽化透明、麻线尺度、半藏程度、
  留白、综合色温和光照措辞。对象数量／身份、格位、实心 fold、辫绳、
  把手式 closure 或背景错误优先从锁定参考重新生成；仅当六件结构门禁
  全部通过而问题局限于局部材质、边缘或综合色时，才允许把 B 的前次输出
  作为 B 的 edit 输入。

### 批次通过与停止条件

- A／B 可分别通过对象级内审，但批次只有在 `676 × 440` 真实层序重组、
  3／5／7 针脚站、真实中文安全区以及最小／基准／最大拉伸检查全部通过后，
  才能进入 `candidate-reviewed / P3` 并交给用户复审。
- 若组合预演暴露某一目标的问题，只恢复该目标并使用其尚未消耗的预算；
  不重置计数，也不借用另一目标的剩余次数。任一目标第 `5` 次仍失败，或
  两个目标都已无可用次数而批次仍不通过，停止为
  `candidate-rejected / P3 / repair-budget-exhausted`。
- 任一次完整内审通过后立即停止相应循环。内部通过不能创建 tracked source
  或 runtime；仍须用户明确接受具体候选，才能进入 `P4`。
- 新增对象、状态、参考、上传、视觉方向、画布、runtime 几何或改变禁止项
  均超出修复边界，立即停止并退回 `prompt-draft` 等待新授权。

### V3.2-A 尝试表

| 尝试 | 正文版本／执行前 commit | 操作 | session／result | 输出／SHA | 第一失败门禁 | 保留区域与下一步 | 结论 |
|---:|---|---|---|---|---|---|---|
| 1/5 | `V3.2-A` / `32b0d15` | generate | 无固定子会话；父进程 `49122` | 无输出／SHA | `0. 执行传输`：npm `FetchError / EPERM`，无法访问 registry | 无图可保留；以完全相同正文经允许网络路径重新 generate | 失败，计入预算 |
| 2/5 | `V3.2-A.r1` / `4748683` | generate | `019fb0d7-3cbf-7c63-9c47-cec5d3bd24cd`／`ig_02ae2756e64bac4d016a6ab626f1e081919591e7a0578374f9` | `attempt-02/...A_PAGES.raw.png`／`49bc19f8379067f7af279888db67a262e4d8015924def543ec3a1c634f44c913` | `4. 美术一致性`：两页中心至边缘均为细密重复压花，非安静素纸；背景亦非均匀纯绿 | 保留恰好两页、分格、近等宽、相反内缘；大面积缺陷，重新 generate | 失败，计入预算 |
| 3/5 | `V3.2-A.r2` / `2c96205` | generate | `019fb0db-4526-7c61-b844-72e12d5add13`／`ig_016fd07a7fadd756016a6ab73013f081918b986e58fbfbd9bd` | `attempt-03/...A_PAGES.raw.png`／`21be07bc0716b725023369dfc6f7887ab8ef23543c188453ef9233450e35e512` | `4. 美术一致性`：细密满页压花仍覆盖中央；次要为相向内缘仍像普通撕边 | 保留两页库存、单元、尺寸和外侧轮廓；改为编辑两页中央与相向内缘 | 失败，计入预算 |
| 4/5 | `V3.2-A.r3` / `782cde6` | edit | `019fb0df-0489-77b3-8c19-8102ba539aae`／`ig_012937f5299e5da0016a6ab83bed148191b3f5ca798c7cd07b` | `attempt-04/...A_PAGES.raw.png`／`1b4eb91a6b7c9670ea2f1c23fa94221ab1aaa229ac1552d659c42f7ab8f91277` | `4. 美术一致性`：中央原始分辨率仍能辨认连续压花；相向内缘仍有撕裂齿 | 保留库存、位置、外缘与已改善的低频综合色；用坐标化实色覆盖中央并再次平滑内缘 | 失败，计入预算 |
| 5/5 | `V3.2-A.r4` / `06f873a` | edit | `019fb0e3-5bb6-7bd2-8504-c9f7b7c936fb`／`ig_0f5af795b1e4056e016a6ab93fe49c8191949983d1c961324b` | `attempt-05/...A_PAGES.raw.png`／`9de577d576719045467f3ef0ac33b64630bd0da63e66c77d38c1e549d6e0230f`；透明候选／`47669c2d5c8243d47bc08cbe417be07bf74121a8a0e2aa8cae749ac109a106d8` | 无目标级失败；综合色与对象语义通过，批次装配尚待 B | 保留两张纸页全部；内缘最终表现由 B 的 fold 叠加后复核，不再调用 A | 目标级内审通过，循环停止 |

#### V3.2-A.r1 — TRANSPORT RETRY

为 Turtle WoW 1.18.1 制作《魔兽世界》60 级香草时代视觉语言的二维手绘
位图组件源画布。本次只生成“冒险者公会任务卷宗”内部的两张单层顶页：
`QUEST.LOG.LIST.PAPER` 与 `QUEST.LOG.DETAIL.PAPER`。不要生成完整界面、
封皮、外围页叠、页沟、装订、按钮或任何第三个对象。

Image 1 是用户锁定的任务详情视觉基准，是最高视觉权威。严格继承它首先像
正式公会双页任务卷宗的物件身份、香草魔兽粗厚略不规则的二维手绘轮廓、
低饱和暖赭与旧象牙纸色、左上暖光、材料厚度和大面积安静阅读纸面；忽略
其中全部任务行、伪文字、标题、按钮、滚动条、徽记、蜡封、书签、奖励框、
黄铜件和封皮。

Image 2 是已经接受的 QL-A1 结构 source，只负责两页在同一本书中的浅俯视
方向、近等宽物理关系、相邻封皮综合色温和内外页缘朝向。不要复制 Image 2
的满页细碎压花、现有中央针脚、页沟、外围多层页叠、厚封皮或黄铜包角。
Image 2 不得覆盖 Image 1 的综合色感、安静纸面和绘制语言。不要使用任何
QL-A2 失败候选作为输入。

输出 `1536 × 1024`、无文字的组件源画布，恰好包含两个互不接触的对象。
左侧 `768 × 1024` 隐形单元只放左页，右侧同尺寸单元只放右页；两对象使用
相同可见外接宽高，四周保留至少 `32px` 连续裁切间距。左页专用内缘位于
右侧并向未来页沟自然弯曲，右页专用内缘位于左侧并形成正确镜像。两页共享
相同浅俯视表面方向、纸张批次、厚度尺度和左上光源。物理页宽近 1:1，绝不
通过拉宽右页表达 `42%／58%` 的文字列比例。

每个对象只是一张单层顶页，不是多层纸堆。外缘可有克制的手绘毛边、短接触
阴影和少量非镜像磨损，但不得在外侧或底部生成三至五层重复页叠。左页略暗
暖赭，右页略亮旧象牙，综合色差很小，仍明显属于同一批纸。

每页中央至少 `85%` 的阅读面积必须接近素纸，只允许大尺度、低频、边界模糊
的云状斑驳，极轻且不可重复的纸纤维，以及缓慢综合色差。中央不得出现任何
可辨认花纹。绝对禁止满页均匀压花、浅浮雕字符、符文状细纹、卷草、网纹、
密集裂纹、颗粒噪声、重复斑点或照片级古纸纹理。高频磨损只能集中在不可
拉伸的外缘、页角和专用内缘。

保持 2004 年前后香草魔兽手绘位图感：宽面笔触、明确但克制的明暗切面、
可见手工误差和温暖奇幻气质。禁止现代卡片、细金属线、透明黑板、照片级
古董、暗黑祭坛、聊天框单页轮廓，以及任何文字、任务行、图标、页码、
书签、按钮、滚动条、针脚、绳结、封皮或完整 UI。

背景优先真正 RGBA 透明；若无法可靠透明，只能使用完全均匀的纯
`#00FF00`。绿色背景上不得有渐变、噪声、纹理、光晕、环境阴影或投影。
最终自检：画面只有两张单层纸页；两页外接尺寸相同、内缘方向相反；中央
没有满页高频纹样；没有外围页叠或第三个对象；背景是真透明或均匀纯绿。

修复说明：attempt 1 未进入固定 Codex 子会话，失败原因仅为 npm 网络传输
`FetchError / EPERM`，没有图像缺陷可修正。为保持用户授权正文与冻结合同，
本 `.r1` 的图像指令与 V3.2-A 完全相同，只改变外部执行通道。

#### V3.2-A.r2 — QUIET PAPER REGENERATION

为 Turtle WoW 1.18.1 制作《魔兽世界》60 级香草时代视觉语言的二维手绘
位图组件源画布。本次只生成“冒险者公会任务卷宗”内部的两张单层顶页：
`QUEST.LOG.LIST.PAPER` 与 `QUEST.LOG.DETAIL.PAPER`。画面必须恰好只有这
两个互不接触的非交互对象；不要生成完整界面、封皮、外围页叠、页沟、装订、
按钮或任何第三个对象。

最高执行优先级如下，后面的美术描述不得覆盖这三项：
1. 恰好两张单层纸页，分别位于固定左右单元，外接尺寸相同。
2. 每页中央至少 `85%` 必须是视觉上接近纯色的哑光素纸，没有任何可辨认、
   可重复或均匀铺满的表面花纹。
3. 背景必须是真 RGBA 透明；若只能色键，则必须是逐像素均匀的纯
   `#00FF00`，纸页外不得存在阴影、光晕、综合色渐变或噪声。

Image 1 是用户锁定的任务详情视觉基准，是最高视觉权威。严格继承它首先像
正式公会双页任务卷宗的物件身份、香草魔兽粗厚略不规则的二维手绘轮廓、
低饱和暖赭与旧象牙纸色、左上暖光、材料厚度和安静阅读纸面；忽略其中全部
任务行、伪文字、标题、按钮、滚动条、徽记、蜡封、书签、奖励框、黄铜件
和封皮。

Image 2 是已经接受的 QL-A1 结构 source，只能作为几何剪影参考：读取两页
在同一本书中的浅俯视方向、近等宽物理关系、相邻封皮综合色温和内外页缘
朝向。生成前在概念上把 Image 2 两页内部视为被纯色遮罩覆盖；绝对不要采样、
临摹或重建 Image 2 的满页细碎压花、浅浮雕、现有针脚、页沟、外围页叠、
厚封皮或黄铜包角。Image 2 不得覆盖 Image 1 的综合色感、安静纸面和绘制
语言。不要使用任何 QL-A2 失败候选作为输入。

输出 `1536 × 1024`、无文字的组件源画布。左侧 `768 × 1024` 隐形单元
只放左页，右侧同尺寸单元只放右页；两对象使用相同可见外接宽高，四周保留
至少 `32px` 连续裁切间距。左页专用内缘位于右侧并向未来页沟自然弯曲，
右页专用内缘位于左侧并形成正确镜像。两页共享相同浅俯视表面方向、纸张
批次、厚度尺度和左上光源。物理页宽近 1:1，绝不通过拉宽右页表达
`42%／58%` 的文字列比例。

每个对象只是一张单层顶页，不是多层纸堆。外缘可有克制的手绘毛边、短接触
阴影和少量非镜像磨损，但不得在外侧或底部生成三至五层重复页叠。阴影必须
收在纸页对象自身的 Alpha 边缘内，不能污染背景。左页略暗暖赭，右页略亮
旧象牙，综合色差很小，仍明显属于同一批纸。

纸页中央必须像尚未书写的厚实公会档案纸：大块、平静、哑光、低对比。
允许的综合色变化只能是边界模糊的大尺度云状斑驳，每一处变化的尺度至少约
为单页宽度的 `15%`；允许极轻、稀疏、无方向性的纸纤维，但在画面缩小到
`25%` 时必须完全不可辨认。中央不得出现任何小于这一尺度的连续装饰节奏。

绝对禁止从纸页中心到边缘铺满的压花、浅浮雕字符、符文状细纹、卷草、
交织结、网纹、蜂窝、织物纹、密集裂纹、颗粒噪声、重复斑点、照片级古纸
纹理或任何“为了显得古老”而添加的细密图案。attempt 2 出现过遍布两页的
浅色重复压花；本次必须彻底消失，而不是仅降低对比度。高频磨损只能集中在
不可拉伸的最外页缘、页角和专用内缘，不能进入中央阅读区。

保持 2004 年前后香草魔兽手绘位图感：宽面笔触、明确但克制的明暗切面、
可见手工误差和温暖奇幻气质。禁止现代卡片、细金属线、透明黑板、照片级
古董、暗黑祭坛、聊天框单页轮廓，以及任何文字、任务行、图标、页码、
书签、按钮、滚动条、针脚、绳结、封皮或完整 UI。

背景优先真正 RGBA 透明。若无法可靠透明，只能使用完全均匀的纯
`#00FF00`；背景所有像素必须同色，不得有渐变、噪声、纹理、光晕、环境
阴影或投影。

最终自检：画面恰好只有两张单层纸页；两页外接尺寸相同、互不接触、内缘
方向相反；缩小到 `25%` 检视时中央看起来是无图案的平静素纸；没有满页
压花、外围页叠或第三个对象；纸页外背景是真透明或逐像素均匀纯绿。若中央
仍能看见重复花纹，则结果不合格，必须移除花纹后再输出。

修复说明：attempt 2 的对象数量、左右分格、近等宽和相反内缘可保留为目标，
但其细密压花遍布两页，属于大面积美术合同失败，不使用该图作为 edit 输入。
本 `.r2` 继续只上传冻结的 Image 1／2，并从锁定权威重新 generate。

#### V3.2-A.r3 — INTERIOR AND INNER-EDGE EDIT

编辑 Image 3，为 Turtle WoW 1.18.1 制作《魔兽世界》60 级香草时代视觉
语言的二维手绘位图组件源画布。Image 3 是本次唯一编辑底图；最终仍只包含
“冒险者公会任务卷宗”内部的两张单层顶页：
`QUEST.LOG.LIST.PAPER` 与 `QUEST.LOG.DETAIL.PAPER`。不要生成完整界面、
封皮、外围页叠、页沟、装订、按钮或任何第三个对象。

严格保留 Image 3 中已经正确的部分：`1536 × 1024` 画布；左页只在左侧
`768 × 1024` 单元、右页只在右侧同尺寸单元；两页互不接触、外接尺寸近似
相同、物理宽度近 1:1；两页顶部、底部和远离页沟的外侧轮廓、页角位置、
整体暖赭／旧象牙色差以及左上暖光。不要移动、缩放、旋转、增加或删除纸页。

本次只重绘两个明确区域：
1. 每张纸页距离可见页缘约 `48px` 以内的中央纸面，约占单页面积 `85%`。
2. 左页朝右的相向内缘与右页朝左的相向内缘，以及紧邻它们的窄幅弯曲带。

中央纸面必须全部替换为视觉上无图案的哑光素纸。彻底擦除 Image 3 当前
遍布两页的浅色压花、细碎重复纹样、浅浮雕、卷草、交织结、网纹、颗粒和
密集裂纹；不是把它们降低对比度，而是完全移除。用宽面手绘笔触和低频综合色
重建纸面：只允许边界模糊的大块云状斑驳，每块尺度至少约为单页宽度的
`15%`，以及极轻、稀疏、缩小到 `25%` 后不可见的无方向纸纤维。中央应像
尚未书写的厚实公会档案纸，大块、平静、哑光、低对比，足以承载长中文。

重新区分相向内缘和普通外缘。左页右边缘要成为向未来中央页沟轻微下弯的
装订内缘：轮廓较连续、略向下收，有一条窄纸面高光和朝页沟渐深的接触阴影；
右页左边缘是它的正确物理镜像。两条相向内缘不得继续像远侧外缘那样破碎、
撕裂或等距毛边，也不得生成独立纸条、实心 fold、页沟、针脚或绳结。它们
仍属于各自单层顶页，未来会由独立 fold 组件覆盖。

Image 1 是用户锁定的任务详情视觉基准，是最高视觉权威。继承正式公会双页
任务卷宗的物件身份、香草魔兽粗厚略不规则的二维手绘轮廓、低饱和暖赭与
旧象牙纸色、左上暖光、材料厚度和安静阅读纸面；忽略其中全部文字、任务行、
按钮、滚动条、徽记、蜡封、书签、奖励框、黄铜件和封皮。

Image 2 是已接受的 QL-A1 结构 source，只负责浅俯视方向、近等宽关系和
相向内缘朝向。把 Image 2 的纸面内部视为被纯色遮罩覆盖，不得采样或重建
它的满页压花、针脚、页沟、外围页叠、封皮或包角。Image 2 不能覆盖
Image 1 的美术权威，也不能改变 Image 3 已冻结的画布和对象位置。

保持 2004 年前后香草魔兽手绘位图感：宽面笔触、明确但克制的明暗切面、
可见手工误差和温暖奇幻气质。禁止现代卡片、细金属线、透明黑板、照片级
古董、暗黑祭坛、聊天框单页轮廓，以及任何文字、图标、页码、书签、按钮、
滚动条、针脚、绳结、封皮或完整 UI。

纸页外背景优先真正 RGBA 透明。若保留色键，必须统一为逐像素均匀的纯
`#00FF00`；不得在纸页外增加阴影、光晕、综合色渐变或噪声。纸页自身的
短接触阴影只能收在可见对象边缘内。

最终自检：仍恰好只有原位置的两张单层纸页；顶部、底部和远侧外缘基本保持；
两页中央缩小到 `25%` 时看起来是完全无图案的平静素纸；左页右内缘与右页
左内缘连续、相向弯曲且互为镜像；没有外围页叠、页沟、装订、第三对象或
动态内容；背景是真透明或均匀纯绿。

修复说明：attempt 3 连续复发满页压花，但其两页库存、单元、近等宽和外侧
轮廓可保留。依据冻结修复边界，本 `.r3` 使用 attempt 3 作为同目标 edit
输入，只重绘纸页中央材质与两条相向内缘；Image 1／2 的职责不变。

#### V3.2-A.r4 — FLAT INTERIOR FINAL EDIT

编辑 Image 3，为 Turtle WoW 1.18.1 制作《魔兽世界》60 级香草时代视觉
语言的二维手绘位图组件源画布。Image 3 是本次唯一编辑底图；最终仍只包含
“冒险者公会任务卷宗”内部的两张单层顶页：
`QUEST.LOG.LIST.PAPER` 与 `QUEST.LOG.DETAIL.PAPER`。不要生成完整界面、
封皮、外围页叠、页沟、装订、按钮或任何第三个对象。

严格保留 Image 3 中已经正确的内容：`1536 × 1024` 画布；左页和右页各自
固定在一个 `768 × 1024` 单元；两页互不接触、外接尺寸近似相同、物理宽度
近 1:1；两页顶部、底部、远侧外缘、页角、整体暖赭／旧象牙色差和左上暖光。
不要移动、缩放、旋转、增加或删除纸页。

这是一次明确区域的覆盖式编辑，不是降低纹样对比度：

1. 用不透明、无纹理的宽面手绘实色完全覆盖左页画布坐标约
   `x=150..690, y=140..880`。
2. 用不透明、无纹理的宽面手绘实色完全覆盖右页画布坐标约
   `x=850..1390, y=140..880`。
3. 重绘左页 `x=690..742` 附近朝右的相向内缘，以及右页
   `x=804..856` 附近朝左的相向内缘。

两个中央矩形内不得保留、透出或重新生成 Image 3 的任何细小表面信息。
不要模拟纸纤维，不要添加纸张颗粒，不要添加装饰性古旧纹理。左页中央以
低饱和暖赭 `#B8955C` 附近的哑光宽面笔触为主，右页中央以旧书页高光
`#D2B77E` 附近的哑光宽面笔触为主；综合色可以偏离这些近似色以衔接原图，
但只能使用每页两到三块跨度超过单页宽度 `25%` 的柔和大色块。中央表面在
`100%` 原始分辨率下也必须完全看不到重复符号、压痕、浮雕、纹样或纤维。

中央应像游戏美术师用大号不透明画笔重新铺过的厚实空白档案纸，而不是照片
材质、程序纹理或带底纹的复古纸。平静、连续、低对比优先于“真实纸张细节”。
所有年代感只由综合色、宽面明暗和外缘磨损承担；中央不承担任何高频年代感。

左页右内缘必须改为较连续、向未来页沟轻微下弯的装订内缘：只有一条窄纸面
高光和朝页沟渐深的接触阴影；右页左内缘是正确镜像。清除两条相向内缘当前
连续排列的撕裂齿、等距缺口和外缘式毛边。不要生成独立纸条、实心 fold、
页沟、针脚、绳结或多层页叠；这两条内缘仍属于各自单层顶页。

Image 1 是用户锁定的任务详情视觉基准，是最高视觉权威。只继承正式公会
双页任务卷宗身份、香草魔兽粗厚略不规则的二维手绘轮廓、低饱和暖色、
左上暖光和材料厚度；忽略其中全部文字、任务行、按钮、滚动条、徽记、蜡封、
书签、奖励框、黄铜件和封皮。

Image 2 是已接受的 QL-A1 结构 source，只负责浅俯视方向、近等宽关系和
相向内缘朝向。Image 2 的纸面内部本次完全无效，不得从中采样任何纹理、
压花、针脚、页沟、页叠、封皮或包角。Image 2 不能改变 Image 3 已冻结的
画布、对象位置和远侧轮廓。

保持 2004 年前后香草魔兽手绘位图感：宽面笔触、明确但克制的明暗切面、
可见手工误差和温暖奇幻气质。禁止现代卡片、细金属线、透明黑板、照片级
古董、暗黑祭坛、聊天框单页轮廓，以及任何文字、图标、页码、书签、按钮、
滚动条、针脚、绳结、封皮或完整 UI。

纸页外背景优先真正 RGBA 透明。若保留色键，必须统一为逐像素均匀的纯
`#00FF00`；不得增加阴影、光晕、综合色渐变或噪声。纸页自身短接触阴影
只能收在对象边缘内。

最终按 `100%` 原始分辨率自检：仍恰好只有原位置的两张单层纸页；两个指定
中央矩形是无任何纹样和纸纤维的哑光宽面素纸；左页右内缘与右页左内缘连续、
相向弯曲且无等距撕裂齿；顶部、底部和远侧外缘保持；没有外围页叠、页沟、
装订、第三对象或动态内容；背景是真透明或均匀纯绿。若中央还能看见任何
重复小图形，不要降低其对比度，必须用不透明实色彻底覆盖后再输出。

修复说明：attempt 4 的大尺度综合色已改善，但中央仍残留可辨认压花，内缘
仍有撕裂齿。依据冻结修复边界，本 `.r4` 使用 attempt 4 作为同目标 edit
输入，以明确画布坐标覆盖中央并平滑相向内缘；Image 1／2 职责不变。

### V3.2-B 尝试表

| 尝试 | 正文版本／执行前 commit | 操作 | session／result | 输出／SHA | 第一失败门禁 | 保留区域与下一步 | 结论 |
|---:|---|---|---|---|---|---|---|
| 1/5 | `V3.2-B` / `02baed7` | generate | `019fb0e9-cf6f-7570-9734-e78f59a1cb6e`／`ig_0f8f8cb738f1a634016a6abae50c14819193381f01bb8c04c6` | `attempt-01/...B_GUTTER.raw.png`／`b96d7e140cec7de62c8acbf9053597dd04b4cbf8da99cf2c164d292ca99c5a03` | `2. 物理语义`：stitch 是约 `38%` 格宽的双股辫绳；top／bottom 是完整外露结且夹带竖向纸缘 | 保留六格顺序、underlay 和单侧 folds 的方向目标；对象身份错误，不上传本图，只用 Image 1／2 重新 generate | 失败，计入预算 |
| 2/5 | `V3.2-B.r1` / `9f76ca3` | generate | `019fb0ed-f09a-7031-97ab-70e45adb37eb`／`ig_023f6fac323dc986016a6abbf3419081919ecd54a6ab69249c` | `attempt-02/...B_GUTTER.raw.png`／`eac0c962d8d1fce0d280e74c8776db290e45d0bf66464ceb808a2f164aed3bb5` | `1. 对象／格位合同`：左右 fold 越过 `y=512` 进入下排；随后 stitch 仍为约 `45%` 格宽辫绳，closure 仍为圆球状完整结和长尾 | 保留六对象角色和收口已不夹带纸缘这一方向；格位与物件身份均失败，不上传本图，重新 generate | 失败，计入预算 |
| 3/5 | `V3.2-B.r2` / 本次提交 | generate |  |  |  |  | 已计数，调用中 |
| 4/5 | `V3.2-B.r3` / 待生成 | edit／generate |  |  |  |  | 未执行 |
| 5/5 | `V3.2-B.r4` / 待生成 | edit／generate |  |  |  |  | 未执行 |

#### V3.2-B.r1 — UNBRAIDED STITCH AND BARE HALF-CLOSURES REGENERATION

为 Turtle WoW 1.18.1 制作与同一册“冒险者公会任务卷宗”配套的二维手绘
页沟组件源画布。本次只生成六个独立非交互对象：
`GUTTER.UNDERLAY`、`GUTTER.LEFT_FOLD`、`GUTTER.RIGHT_FOLD`、
一个 `GUTTER.STITCH`、`GUTTER.TOP` 和 `GUTTER.BOTTOM`。不要生成纸页、
完整书本、封皮、按钮或任何第七个对象。

以下三项是本次最高执行优先级，后面的美术描述不得覆盖：

1. 下排左格的 stitch 必须是一段短、细、单股、无编织纹的横向麻线弧段，
   可见长度为 `120–160px`，绝不超过 `179px`；表面不能出现交替斜纹、
   X 形纹、鱼骨纹、双股缠绕或辫绳结构。
2. 下排中格与右格的 closure 各自只能包含一枚不完整、被想象中的纸缘遮住
   大半的半结残段和一条短尾。两个格内绝对不能生成纸张、竖向纸缘、fold、
   皮条、底板或其他用来表现“遮住”的实体；遮挡只通过截断结的剪影表达。
3. 六个对象必须严格留在各自固定格内，四周至少 `48px`；背景必须是真
   RGBA 透明，若只能色键则必须是逐像素均匀纯 `#00FF00`。

Image 1 是用户锁定的任务详情视觉基准，是最高视觉权威。继承它的正式公会
任务卷宗身份、香草魔兽宽面手绘笔触、低饱和暖赭／烟褐／深胡桃色、粗麻线
尺度、左上暖光和克制装订语义；忽略其中所有文字、任务行、按钮、滚动条、
徽记、蜡封、书签、奖励框和黄铜装饰。

Image 2 是已接受的 QL-A1 结构 source，只负责内部浅俯视方向、两页之间的
接触尺度、纸缘厚度和相邻综合色温。不要复制 Image 2 的现有中央针脚、
页沟、满页压花、外围页叠、封皮或包角。Image 2 不能决定本批六个对象的
形态。不要使用任何 QL-A2 失败候选作为输入；尤其不要使用 attempt 1，
它只作为本正文书面记录中的负面证据。

输出 `1536 × 1024`、无文字的组件源画布，使用固定 `3 × 2` 隐形网格，
每格 `512 × 512`。上排从左到右恰好是 underlay、left fold、right fold；
下排从左到右恰好是单个 horizontal stitch、top closure、bottom closure。
每个对象留在自己单元中央，距单元四边至少 `48px`；六组互不接触、不共享
阴影、不通过噪声相连。不要生成标签、分隔线、单元边框或第七组装饰。

上排左侧 underlay 大部分透明，只包含一条窄而柔和、向中央稍深的凹陷接触
阴影和极低对比深胡桃衬布色。它的可见高度约 `300–400px`，两侧长距离
羽化到透明，纵向明暗只有缓慢变化。它没有完整硬边、矩形剪影、等宽黑褐
竖条、可辨识表面花纹、编织网格、辫绳、拉链齿、皮革竖带或外封脊，也不
投下独立阴影。

上排中间 left fold 是左页内缘的一条单侧弯曲覆盖，可见高度不超过
`400px`：只保留一条纸张高光、轻微内弯综合色差和接触阴影，其余区域向
同一侧长距离羽化透明。单个对象只能有一条可辨认纸边，没有完整上边、
下边、第二条纵向纸边、毛边底部、外侧页叠、封闭轮廓或实心矩形。上排
右侧 right fold 是它在物理方向上的正确镜像，同样只有一条纸边，不能
成为独立纸条。两个 fold 的纸色和 attempt 5 双页属于同一批暖赭／旧象牙
纸，但本次不上传或复制该双页。

下排左侧 stitch 只是一段中央略拱的单股粗麻线，不是完整绳索。它宽约
`120–160px`、厚约 `12–20px`，左右端逐渐变细、变暗并消失到透明。
线体内部只允许两三笔沿长度方向的手绘纤维高光，不能交叉、编织或形成
周期性斜纹；没有双股结构、圆头、孔环、绳结、底板、矩形阴影、投影或
纵向绳段。多个副本未来沿中心离散重复时，必须仍读作数个横向短针脚。

下排中间 top closure 是一枚宽高约 `56–96px` 的不规则半结残段，只画出
纸缘下会露出的半个松结和一条很短的麻线尾；结体的一侧像被画面内不可见的
纸缘自然截断。下排右侧 bottom closure 使用相同麻线尺度，但截断方向与
短尾姿态不同，不能机械复制。两格都只画麻线，不画负责遮挡它的纸缘或 fold。
它们不是完整外露绳结、蝴蝶结、U 形扣、横向把手、桥、横梁、杆件或两端
带球的装饰。

六个对象共享同一浅俯视方向、左上暖光、低饱和综合色温、纸／麻线／衬布
厚度尺度和 2004 年前后香草魔兽二维手绘位图精度：宽面笔触、明确但克制
的明暗切面、略有手工误差和温暖奇幻气质。禁止照片级绳索、现代图标、
黑铁结构、霓虹、细金线、文字、完整纸页、任务行、按钮、滚动条、封皮或
完整 UI。

背景优先真正 RGBA 透明；若无法可靠透明，只能使用完全均匀的纯
`#00FF00`。绿色背景上不得有渐变、噪声、纹理、光晕、环境阴影或投影；
对象自身的柔和羽化只允许向透明 Alpha 过渡，不能把深浅绿色画进背景。

最终自检：固定六格中各有且只有指定对象；所有对象距格边至少 `48px`；
underlay 是柔和凹陷阴影而非完整深色竖条；两个 fold 各只有一条纸边；
stitch 长度不超过 `179px`、无任何辫纹且没有圆头；两个 closure 都只是
不带纸缘的半藏麻线残段；没有纸页或第七个对象；背景是真透明或逐像素均匀
纯绿。若下排出现辫绳、完整绳结或任何竖向纸边，结果不合格，必须移除这些
结构后再输出。

修复说明：attempt 1 的六格顺序、underlay 方向以及左右单侧 fold 的大方向
可作为目标，但该图不作为输入。其 stitch 长约格宽 `38%` 且有清晰双股
辫纹；top／bottom closure 是完整外露结，并各自夹带一条竖向纸缘。依据
冻结修复边界，本 `.r1` 只上传 Image 1／2，从锁定权威重新 generate。

#### V3.2-B.r2 — FIXED BOUNDING BOXES AND UNTWISTED LINEN REGENERATION

为 Turtle WoW 1.18.1 制作与同一册“冒险者公会任务卷宗”配套的二维手绘
页沟组件图集。本次只生成六个相互隔离、没有交互状态的透明 decal：
`GUTTER.UNDERLAY`、`GUTTER.LEFT_FOLD`、`GUTTER.RIGHT_FOLD`、
一个 `GUTTER.STITCH`、`GUTTER.TOP` 和 `GUTTER.BOTTOM`。不要生成纸页、
完整书本、封皮、按钮、标签、分隔线或任何第七个对象。

先把 `1536 × 1024` 画布严格划成六个不可越界的 `512 × 512` 单元。
每个单元只有中央安全矩形可以出现非背景像素；任何对象、羽化、阴影或尾端
都不得越出它自己的安全矩形：

- 上排左格 underlay：全局 `x=196..316, y=80..432`。
- 上排中格 left fold：全局 `x=708..828, y=80..432`。
- 上排右格 right fold：全局 `x=1220..1340, y=80..432`。
- 下排左格 stitch：全局 `x=176..336, y=730..806`。
- 下排中格 top closure：全局 `x=728..808, y=730..806`。
- 下排右格 bottom closure：全局 `x=1240..1320, y=730..806`。

这些坐标是硬边界，不是大致建议。上排对象绝不能触碰或越过 `y=512`；
下排对象不能伸入上排。每个安全矩形之外必须只有真透明或均匀纯绿色键。

以下物理身份与硬边界同为最高优先级：

1. stitch 不是绳子，也不是经过捻合的细绳。它是一根未编织、未捻合的
   单根粗亚麻线：连续平滑横向弧段，宽 `120–150px`、厚 `12–18px`。
   轮廓内只能有沿线长方向的两三笔平行纤维高光；绝对没有对角条纹、
   X 形、螺旋、鱼骨纹、两股相绕、辫纹、圆头或绳结。
2. top／bottom closure 不是“完整结缩小版”。每个只是一块宽高
   `48–72px` 的不完整麻线弯折片段：一侧轮廓被截断，只有一次不闭合的
   急弯和不超过 `20px` 的短尾。没有圆球、闭合圆环、中心孔、完整结体、
   蝴蝶结、长尾或任何纸缘。
3. left／right fold 都只是单侧纸缘的透明渐变遮罩，不是纸条。每件只有
   一条硬纸边；其余三侧全部软化消失，不能形成第二条纵边、上边或下边。

Image 1 是用户锁定的任务详情视觉基准，是最高视觉权威。继承它首先像正式
冒险者公会任务卷宗的物件身份、2004 年前后香草魔兽宽面手绘位图笔触、
低饱和暖赭／烟褐／深胡桃色、克制麻线尺度、左上暖光、略有手工误差的
轮廓和温暖奇幻气质；忽略其中所有文字、任务行、按钮、滚动条、徽记、蜡封、
书签、奖励框、黄铜装饰与完整纸页。

Image 2 是已接受的 QL-A1 结构 source，只负责内部浅俯视方向、两页接触
尺度、纸缘厚度和相邻综合色温。不要复制 Image 2 的中央针脚、页沟、
满页压花、外围页叠、封皮或包角。Image 2 不能决定六个 decal 的形态，
也不能覆盖 Image 1 的绘制语言。不要使用任何 QL-A2 失败候选作为图像输入；
attempts 1／2 只作为本正文中的负面文字证据。

上排左格 underlay 大部分透明，只是一条窄、柔和、中央稍深的凹陷接触阴影，
带极低对比深胡桃衬布色。它宽约 `40–80px`、高约 `280–340px`，左右
长距离羽化，纵向明暗只有缓慢变化。它没有硬矩形轮廓、完整明边、等宽黑褐
竖条、表面花纹、编织网格、辫绳、拉链齿、皮革竖带或外封脊。

上排中格 left fold 高约 `280–340px`，只保留左页朝页沟的一条纸张高光、
轻微内弯综合色差和接触阴影；从唯一硬纸边向左侧长距离羽化透明。上排右格
right fold 是物理正确镜像，从唯一硬纸边向右侧羽化透明。两者分别只有一条
纵向纸边，没有实心矩形、封闭轮廓、独立顶部、独立底部、第二条纵边、
多层页叠或撕裂底边；纸色属于同批暖赭／旧象牙任务纸。

下排左格 stitch 的中心位于自身格的正中。它是哑光、单根、未捻合的粗亚麻
线，不要使用“rope”或“braid”的视觉语言。左右端逐渐变细、变暗并消失到
透明；线体只有顺着横向弧度的宽面高光，没有任何周期性表面节奏。多个副本
未来纵向离散放置时，必须仍读作多个短横针脚，不能连成竖向构件。

下排中格 top closure 只画一个不闭合的亚麻线急弯残段：左侧或上侧像被
不可见纸缘截断，露出一个不完整弯折和极短尾。下排右格 bottom closure
使用相同线材尺度，但截断方向相反、短尾姿态不同。两者轮廓紧凑、扁平、
不规则，不是圆球；格内不画用于遮挡的纸张、fold、底板或投影。

六件共享同一浅俯视表面方向、左上暖光、低饱和综合色温和手绘厚度尺度。
材料必须能区分：纸缘厚而哑光，亚麻线略粗糙但不编织，underlay 柔和而
低对比。禁止照片级纤维、现代图标、黑铁、霓虹、细金线、文字、任务行、
按钮、滚动条、完整纸页、封皮或完整 UI。

背景优先真正 RGBA 透明。若无法可靠透明，只能使用完全均匀、逐像素一致的
纯 `#00FF00`；绿色上不得有渐变、噪声、纹理、光晕、环境阴影或投影。
对象的软边应由 Alpha 羽化表达，不得画成深浅绿色烟雾。

最终自检必须逐格进行：六件各自完全位于列出的全局安全矩形；上排没有像素
进入下排；underlay 是窄柔和凹陷；两个 fold 各只有一条纸边；stitch 是
`120–150px` 的无捻合单根亚麻线，内部没有任何斜纹；两个 closure 各是
`48–72px`、无圆环无长尾无纸缘的截断弯折片段；没有第七对象；背景是真
透明或均匀纯绿。只要看到辫纹、圆球完整结、长尾、纸缘进入 closure 格，
或任一上排对象越过 `y=432`，就必须删除错误结构后再输出。

修复说明：attempt 2 不作为输入。它的左右 fold 向下越过 `y=512` 进入
下排，stitch 仍约 `230px` 且保留辫纹，两个 closure 虽去掉纸缘却仍是
圆球状完整结并带长尾。依据冻结边界，本 `.r2` 量化六个安全外接框，把
stitch 明确为未捻合单根粗亚麻线，把 closure 改为不闭合弯折片段，并继续
只上传 Image 1／2 重新 generate。

## 执行记录

- 日期：授权并开始执行于 `2026-07-30`
- 授权版本 commit：`32b0d15`
- V3.2-A 会话／结果 ID：attempt 1 无固定子会话；attempt 2
  `019fb0d7-3cbf-7c63-9c47-cec5d3bd24cd`／
  `ig_02ae2756e64bac4d016a6ab626f1e081919591e7a0578374f9`；attempt 3
  `019fb0db-4526-7c61-b844-72e12d5add13`／
  `ig_016fd07a7fadd756016a6ab73013f081918b986e58fbfbd9bd`；attempt 4
  `019fb0df-0489-77b3-8c19-8102ba539aae`／
  `ig_012937f5299e5da0016a6ab83bed148191b3f5ca798c7cd07b`；attempt 5
  `019fb0e3-5bb6-7bd2-8504-c9f7b7c936fb`／
  `ig_0f5af795b1e4056e016a6ab93fe49c8191949983d1c961324b`
- V3.2-B 会话／结果 ID：attempt 1
  `019fb0e9-cf6f-7570-9734-e78f59a1cb6e`／
  `ig_0f8f8cb738f1a634016a6abae50c14819193381f01bb8c04c6`；attempt 2
  `019fb0ed-f09a-7031-97ab-70e45adb37eb`／
  `ig_023f6fac323dc986016a6abbf3419081919ecd54a6ab69249c`
- 实际输入绝对路径与职责：A attempts 2–5 均上传元数据中固定 SHA 的
  Image 1／2；attempts 4–5 另将同目标前次 raw 作为 Image 3 edit 输入；
  职责与授权正文及冻结修复边界一致
- imagegen 报告的 revised prompt：attempt 1 未进入固定子会话，无
- 输出尺寸／模式／SHA-256：attempt 2 `1536 × 1024 RGB`／
  `49bc19f8379067f7af279888db67a262e4d8015924def543ec3a1c634f44c913`；
  attempt 3 `1536 × 1024 RGB`／
  `21be07bc0716b725023369dfc6f7887ab8ef23543c188453ef9233450e35e512`；
  attempt 4 `1536 × 1024 RGB`／
  `1b4eb91a6b7c9670ea2f1c23fa94221ab1aaa229ac1552d659c42f7ab8f91277`；
  attempt 5 raw `1536 × 1024 RGB`／
  `9de577d576719045467f3ef0ac33b64630bd0da63e66c77d38c1e549d6e0230f`；
  deterministic chroma candidate `1536 × 1024 RGBA`／
  `47669c2d5c8243d47bc08cbe417be07bf74121a8a0e2aa8cae749ac109a106d8`；
  B attempt 1 `1536 × 1024 RGB`／
  `b96d7e140cec7de62c8acbf9053597dd04b4cbf8da99cf2c164d292ca99c5a03`；
  B attempt 2 `1536 × 1024 RGB`／
  `eac0c962d8d1fce0d280e74c8776db290e45d0bf66464ceb808a2f164aed3bb5`
- Alpha／残色：attempt 2 全部 `1572864` 像素不透明；精确 `#00FF00`
  `2259`，启发式绿色主导 `462597`；attempt 3 仍全不透明，精确绿 `209`、
  启发式绿色主导 `510783`；attempt 4 仍全不透明，精确绿 `22`、启发式
  绿色主导 `510391`；attempt 5 raw 仍全不透明，精确绿 `0`、启发式绿色
  主导 `508890`。attempt 5 透明候选含透明／半透明／不透明像素
  `504212／6139／1062513`，未检出绿色残留。B attempt 1 全部
  `1572864` 像素不透明，精确 `#00FF00` 为 `0`、启发式绿色主导
  `1496660`，背景不是均匀色键。B attempt 2 仍全部不透明，精确绿 `8`、
  启发式绿色主导 `1500756`，同样不是均匀色键；两次都因更早结构／语义
  门禁失败，不做 Alpha 清理
- attempt 5 对象外接：左页 `[96,81,745,936]`，右页
  `[799,82,1441,937]`；可见宽分别 `649px`／`642px`，差约 `1.09%`；
  两对象均未触碰画布或单元边界
- 固定执行器调用：A `5/5`；B `3/5`；合计 `8/10`
- 循环终态：V3.2-A 目标级内审通过并停止；V3.2-B attempt 2 内审失败，
  attempt 3 已计数，等待调用结果

## 审查记录

- 范围／对象身份：Prompt 预检通过。A 只允许两张单层纸页，B 只允许六个
  gutter 小件；两个尺度族不再挤在同一画布。
- 语义／物理：Prompt 预检通过。单层纸页不重复 QL-A1 页叠；underlay、
  单侧 fold、短单股 stitch 与半藏 closure 的物理身份已分别写明。
- 透视／图层：两次调用共享 Image 1／Image 2 与浅俯视、左上光；最终层序
  仍按 underlay／stitch／closure → pages → folds → runtime 内容重组。
- 美术一致性：Image 1 继续为最高权威；QL-A1 source 只提供结构；Chat
  图像不再上传，其材料语言只通过书面基线继承，已消除 V3.1 的主要污染源。
- 对象／状态合同：A 恰好两组，B 恰好六组；总计仍对应八个真实非交互对象。
- 装配／尺寸：两个 `1536 × 1024` 画布和 B 的 `3 × 2` 固定网格已定义；
  候选生成后仍必须完成 `676 × 440`、3／5／7 针脚与中文安全区预演。
- 技术像素：attempt 5 raw 的绿色背景不均匀，但语义／物理／美术门禁先行
  通过；使用固定确定性脚本移除色键后，RGBA 候选无绿色残留、对象互不接触
  且未触边。左右可见宽差约 `1.09%`，属于合同“约 `1%`”的边界内误差。
- V3.2-A 结论：attempts 2／3／4 因满页高频压花退回；attempt 5 的中央
  阅读区已成为宽面、低频、无重复图案的安静纸面，恰好两张单层近等宽纸页，
  无第三对象或动态内容。相向内缘仍有克制手绘不规则，但保持相向方向，
  必须在 B 的 `LEFT_FOLD`／`RIGHT_FOLD` 覆盖和 `676 × 440` 重组中再次
  观察，不能单独提升为批次通过。A 当前为目标级内部通过，循环立即停止。
- 批次结论：当前为 `repair-prepared / P3`，尚没有可交给用户复审的完整
  QL-A2 候选。
- V3.2-B attempt 1 结论：六格数量和上／下排角色顺序成立，underlay 与
  两个单侧 fold 的大方向可保留为文字目标；首个失败门禁为物理语义。
  stitch 的可见长度约 `195px`，约占 `512px` 格宽 `38%`，且内部是清晰
  双股辫纹；top／bottom 是完整外露绳结，并错误夹带竖向纸缘。两个 fold
  的底端还逼近格边，小于 `48px` 安全边距。背景为不均匀近绿色。由于对象
  身份尚未通过，不透明化、不重组、不把 attempt 1 作为下一次图像输入。
- V3.2-B attempt 2 结论：仍可辨认为六个预期角色，且两个 closure 已不
  再夹带纸缘；但首个失败门禁提前到对象／格位合同，左右 fold 明显越过
  `y=512` 进入下排。其后 stitch 仍是约 `230px`、约 `45%` 格宽的辫绳，
  两个 closure 仍为圆球状完整结并带长尾。背景也不是均匀色键。结构未过，
  不透明化、不重组、不把 attempt 2 作为下一次图像输入。
- 用户结论与日期：`2026-07-30`，已明确授权 V3.2-A／B 及冻结修复／上传
  范围；这不是候选接受
- 下一门禁：调用固定执行器执行 V3.2-B attempt 3；每次按六件语义、
  固定格位、透明技术合同和真实层序重组依次审查，在冻结边界内最多修复
  至 B attempt 5
