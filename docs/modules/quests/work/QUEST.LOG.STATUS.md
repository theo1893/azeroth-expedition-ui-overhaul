# QL-B3 目录任务状态覆盖 V1

## 元数据

- 模块：Quests／Quest Log 左页目录。
- 批次：`QL-B3`。
- 组件 ID：
  - `QUEST.LOG.TYPE.BADGE`
  - `QUEST.LOG.TIMER.BADGE`
  - `QUEST.LOG.STATE.SEAL`
- 执行正文：
  - `QL-B3-A V1`：四类任务类型压印。
  - `QL-B3-B V1`：一枚限时沙漏压印。
  - `QL-B3-C V1`：完整／破裂两态蜡封。
- 子状态：`repair-prepared`。
- 项目阶段：`P3`。
- 固定执行器：`imagegen-0-143-0`／`@openai/codex@0.143.0`。
- 操作：三段均为首次 `generate`；同一段后续可在冻结边界内选择
  `edit` 或 `generate`。
- 当前实际生图：
  - QL-B3-A：`1/5`
  - QL-B3-B：`0/5`
  - QL-B3-C：`0/5`
- 流程错误：`3`；无候选且无 provider 生成证据时单列，不占上述额度。
- 多执行正文最坏实际生图数：`15`。
- 用户授权：`2026-07-30` 明确授权 `QL-B3-A/B/C V1`；允许每段上传固定
  SHA 的 Image 1／2，允许同一段前次输出仅在冻结边界内作为 edit 输入；
  每段最多 `5` 次实际 ImageGen 调用，最坏合计 `15` 次；流程错误不占
  生图额度。
- 锁定视觉基准：
  - Image 1：
    [`任务详情面板_视觉基准_v1.png`](../../../../assets/locked/quests/任务详情面板_视觉基准_v1.png)，
    SHA-256
    `03dc589abad7187c478ec484cc6565f2c16d2ce52d2d6421251a4de6437453bd`。
- 基准提示词 provenance：
  - [`ART_BASELINE.md`](../ART_BASELINE.md)：Image 1 的任务卷宗物件身份、
    时代、材料关系、综合色、光照、磨损和反模式。
  - [`SUBMODULE_ART_BASELINES.md`](../SUBMODULE_ART_BASELINES.md)：
    左页目录覆盖层及 QL-B3 三类语义的稳定转译。
  - [`GLOBAL_ART_BASELINE.md`](../../../GLOBAL_ART_BASELINE.md)：
    香草时代二维手绘位图、厚重实体材料和全局禁用语言。
- 次级材料参考：
  - Image 2：
    [`QuestLogBookShell_Master_v1.png`](../../../../assets/source/quests/ql-a1/QuestLogBookShell_Master_v1.png)，
    SHA-256
    `91f9fece41ed375df1fa32e94b18797cbb280c0b5e99478862473589c671edd5`；
    只负责当前 runtime 纸张综合色、左上暖光、材料尺度与接触关系，不能覆盖
    Image 1 的最高视觉职责。
- 非上传的内部尺度证据：
  - QL-B1 已接受 `12px`／`10px` 目录墨记只用于审查小尺寸线重与信息密度；
    不作为 Image 3，不要求生成模型模仿其生产偏差。
- raw：无。
- 透明候选：无。
- 真实排版预演：待每次实际生图后，按本文件合同生成。
- 最终 source：无。

## 当前批次边界

QL-B3 只新增三类无鼠标覆盖 Texture，不接管任务行 Button、文字、选择、
追踪、滚动或 SavedVariables。三类状态可以在同一任务行同时出现，不能用
“优先级只显示一枚”的方式丢失信息。

QL-B3 不包含：

- QL-B1 的地区展开／收起和任务追踪墨记；
- QL-B2 的当前任务选择书签；
- QL-C 的 Collapse All、两套 ScrollBar、关闭和操作 Button；
- QL-D 的详情分隔、奖励槽和奖励文字；
- 外部 Quest Tracker 或 NPC Quest／Gossip。

## 美术基准继承

### 权威顺序

1. Image 1 与其在 `ART_BASELINE.md`／`SUBMODULE_ART_BASELINES.md` 中的
   Prompt provenance。
2. Quests 主模块与子模块稳定 Prompt。
3. `docs/GLOBAL_ART_BASELINE.md`。
4. `SUBMODULES.md` 的真实对象、状态、几何、层序与禁止烘焙合同。
5. Image 2 的纸张综合色、光向和材料尺度。
6. QL-B1 已接受小尺寸 runtime 视觉，仅用于离线审查线重。

### 必须继承的视觉 DNA

- 2004 年前后香草魔兽二维手绘位图，不是矢量、网页图标或现代 HUD。
- 轮廓粗厚、略不规则，内部明暗切面有限且清楚；缩至 `10px`／`12px`
  时仍先读出剪影。
- 左上方暖光、低饱和暖赭／烟褐／暗酒红／氧化暗金综合色。
- 墨迹像盖在同一批任务纸面上的公会档案压印；蜡封像 Image 1 中旧蜡材质的
  小型同年代衍生物。
- 磨损集中在凸缘和边缘，主体不堆积照片级裂纹、噪点或精细纹章。

### 本批组件级转译

- 类型章是无底座的深乌棕公会压印：四枚符号同一线重、同一视觉面积，
  通过剪影而不是文字或颜色区分。
- 计时章是同一压印家族中的单枚沙漏，信息强度与类型章相当，不做发光警报。
- 状态章使用一枚小型暗酒红旧蜡封的物理两态：完整与沿同一封印破裂；
  不把 complete／failed 简化成现代绿色勾和红色叉。

### 明确不继承

- 不继承 Image 1 的完整双页书、逐行卡片、顶部指南针横条、页脚大蜡封、
  蓝色飘带、黄铜按钮、分隔线、文字占位或整张布局。
- 不继承 Image 2 的完整书壳、页缘、中央装订、包角和全幅纸面。
- 不从 QL-B1 source 裁切、描摹或复制箭头／墨圈。

### 冲突审计

| 冲突 | 裁决 |
|---|---|
| Image 1 有大号立体黄铜徽章与页脚蜡封；QL-B3 是 `10px`／`12px` 高频行内覆盖 | 只继承年代、材料、线重和综合色；类型／计时采用无背板墨压印，状态蜡封去除飘带并极度简化 |
| 当前任务文字使用 `x=18..208`，QL-B1 追踪墨圈使用 `x=212..222`，三类新状态无空间 | 非地区行固定建立 `x=176..210` 三槽状态带，文字宽度改为 `155px`；标题行继续使用原 `190px` |
| 小图标需要清晰；原型包含大量精细纹章 | 每枚只允许一个主剪影、最多一个大负形或一条结构裂缝；禁止微型刻字和细密内部线 |
| 类型、限时与完成状态可能同时成立 | 三槽全部保留并可同时显示；不叠放、不互相替代、不改变任务文字起点 |
| `questTag` 是本地化返回值，当前离线环境未证明 Turtle WoW 的全部等值 token | 资产可按四个稳定语义生产；runtime 只对实机确认的显式等值映射显示，未知值隐藏，禁止解析任务名或做模糊字符串猜测 |
| 生成模型常把透明资产做成卡片、硬币或网格展示板 | 三段正文都明确要求单独 sprite、纯色键背景、无底座、无格线、无标签和严格对象计数 |

## 组件合同

### 真实行几何与层序

- 目标对象：`QuestLogTitle1..23`，每行 `224 × 15 UI px`，纵向步进
  `14px`。
- QL-B2 选择书签：`BORDER`，局部 `x=-8..15`。
- 非地区任务文字：左边固定 `x=18`，宽度从 `190px` 收敛为 `155px`，
  右边界 `x=173`；`x=174..175` 为安静间隔。
- `QUEST.LOG.TYPE.BADGE`：`10 × 10 UI px`，槽位
  `x=176..186`，垂直居中。
- `QUEST.LOG.TIMER.BADGE`：`10 × 10 UI px`，槽位
  `x=187..197`，垂直居中。
- `QUEST.LOG.STATE.SEAL`：`12 × 12 UI px`，槽位
  `x=198..210`，垂直居中。
- QL-B1 追踪墨圈：`10 × 10 UI px`，槽位 `x=212..222`。
- 地区 header 不显示 QL-B3，保留 `x=18` 起点和 `190px` 文字宽度。
- 层序：SHELL → QL-B2 `BORDER` → QL-B1／QL-B3 `ARTWORK` →
  任务 FontString；QL-B3 Texture 不接收鼠标。
- 任何 API、媒体或映射缺失都只隐藏对应 Texture，不移动其余槽位，
  不阻断原始任务行脚本。

### QL-B3-A — `QUEST.LOG.TYPE.BADGE`

- 逻辑对象：`4` 个，顺序固定为
  `elite`／`dungeon`／`raid`／`pvp`。
- 运行时状态：普通任务与未知 tag 无资产；四类可靠 tag 各显示一格。
- source：`1024 × 1024`，`2 × 2` 等分 atlas；每格
  `512 × 512`，格内中央安全盒 `256 × 256`。
- runtime：确定性逐格可见 bbox 裁切、等比缩放、居中到
  `16 × 16` cell 内的 `10 × 10` content；目标 atlas
  `64 × 16 RGBA TGA`。
- 四枚必须同一无底座墨压印家族；不能依靠颜色差异。

### QL-B3-B — `QUEST.LOG.TIMER.BADGE`

- 逻辑对象：`1` 个 `timed` 沙漏压印。
- source：`1024 × 1024`，物件完全落在中央 `320 × 320` 安全盒。
- runtime：可见 bbox 等比缩放、居中到 `16 × 16` cell 内的
  `10 × 10` content；目标为 `16 × 16 RGBA TGA`。
- 只在 `GetQuestTimers()` 与 `GetQuestIndexForTimer()` 同时存在且绝对
  任务索引命中时显示；不烘焙倒计时数字。

### QL-B3-C — `QUEST.LOG.STATE.SEAL`

- 逻辑对象：`2` 个，顺序固定为 `complete`／`failed`。
- source：`1024 × 1024` 左右二等分；每格 `512 × 1024`，物件落在各自
  中央 `256 × 256` 安全盒。
- 两态必须来自同一枚封印：相同外径、综合色、压纹和磨损位置；failed
  只通过物理断裂与分离表达，不改成另一枚徽章。
- runtime：逐格可见 bbox 裁切、等比缩放、居中到 `16 × 16` cell 内的
  `12 × 12` content；目标 atlas `32 × 16 RGBA TGA`。
- `isComplete == 1` 显示 complete，`isComplete == -1` 显示 failed，
  其他值隐藏。

### 三段共同技术与禁止项

- 第一次调用每段都只上传固定 SHA 的 Image 1／Image 2。
- source 使用均匀纯 `#00FF00` 色键；物件本身不得含绿色、绿色反射、
  绿色描边或落在安全盒外的阴影。
- 生成后才进行确定性归一化、色键转 Alpha、可见 bbox 裁切、等比缩放、
  居中、atlas 装配与 TGA 转换；这些操作不得重画、拉伸、旋转或补造像素。
- 禁止文字、字母、数字、任务名、等级、倒计时、勾叉字符、方形 checkbox、
  圆形硬币底座、金属铭牌、卡片底、按钮框、外发光、投射到色键背景的长阴影。
- 禁止现代扁平图标、移动应用 badge、细金线网页 icon、霓虹、玻璃、
  暗黑 3 尖刺黑铁、骷髅、恶魔角、宝石和镜面黄金。

### API、动态内容与回退

- `GetQuestLogTitle` 继续提供标题、等级、`questTag`、`isHeader`、
  `isCollapsed` 与 `isComplete`；任务名、等级和 pfUI 的现有 `level+`
  文本保持动态，不由资产替代。
- 类型映射只允许显式等值表；未实机确认的 token 不显示类型章。
- 每次刷新先通过计时 API 建立绝对 quest index 集合，再按真实滚动 offset
  绑定可见行；禁止把屏幕行号当绝对任务索引。
- QL-B3 不改变任务选中、追踪、展开、滚动、分享、放弃、退出或
  SavedVariables。
- source／runtime 缺失时隐藏对应新覆盖，保留 QL-A2／B0／B1／B2 当前
  runtime 与原生／pfUI 动态文字。

### 真实排版预演

每个 countable output 都必须生成 `676 × 464`、100% runtime 的真实排版：

- 使用当前 QL-A2 shell、QL-B1 四态和 QL-B2 三态 runtime；
- 实例化全部 `23` 行、真实地区 header、中文任务名、等级、选中行和
  追踪状态；
- 非地区行使用 `155px` 文本宽度，显示长标题截断的最坏情况；
- 分布至少覆盖 normal、四类 type、两条 timed、四条 complete、两条 failed，
  且至少一行同时显示 type＋timer＋state＋tracking 四槽；
- 未完成 QL-C／D 使用当前真实 fallback，若无法取得则明确标注为
  非权威占位；
- 预演只写入 `generated/quests/QL-B3/`，不能晋级 source。

## 生产正文完整性预检

- 复杂度：
  - QL-B3-A：四对象 atlas。
  - QL-B3-B：单对象。
  - QL-B3-C：同一物件的两状态 atlas。
- 结论：三段均 `pass`；没有为生成虚构实机 token，类型 runtime 映射仍保留
  为 P5 前的显式验证门禁。

| 门禁 | 执行正文中的证据 | 结论 |
|---|---|---|
| 物件身份、范围、对象／状态数量、动态内容排除 | 各正文的“任务”和逐格定义；明确 4／1／2 个对象与无文字 | pass |
| 输入图 inherit／ignore 和权威冲突 | 各正文首段逐张声明 Image 1／2 的继承与排除职责 | pass |
| 画布、格位、边距、方向、尺度、光照、层序 | `1024²`、格序、安全盒、正视、左上暖光、无格线均写入正文 | pass |
| 逐对象形态、材料、边缘、状态关系 | 四类剪影、沙漏结构、同一蜡封完整／破裂关系逐项明确 | pass |
| 安全区、裁切、拉伸、重复和接缝 | 每段安全盒、透明隔离、禁止拉伸／镜像／重复均写入正文 | pass |
| 美术 DNA、反模式、色键与最终自检 | 香草手绘、材料层级、具体禁用语言、纯绿和逐项自检完整 | pass |

- 未知但执行必需的值：无。Turtle WoW 的本地化 `questTag` 等值 token
  不影响资产对象与 source 几何，保留为 runtime 集成门禁。
- 去冗余结论：三段不重复仓库历史；只重复各自独立调用必须自包含的
  输入职责、综合色、反模式、色键和高风险对象计数。

## 最终执行正文 — QL-B3-A V1

制作一张只包含四枚《魔兽世界》60 级香草时代任务目录“类型压印”的二维
手绘 UI sprite atlas。它们是盖在旧公会任务纸面上的小型档案墨印，不是四枚
按钮、硬币、金属徽章、任务卡片或独立面板；所有任务名、等级、`Elite`、
`Dungeon`、`Raid`、`PvP` 等文字仍由游戏运行时负责，图中绝不出现任何
文字、字母或数字。

Image 1 是最高视觉权威。继承它的 2004 年前后香草魔兽二维手绘位图语言：
粗厚而略不规则的手工轮廓、有限但明确的亮面／中间面／阴影切面、左上暖光、
低饱和暖赭与烟褐综合色、真实旧墨和克制磨损。只继承年代、笔触、线重、
综合色和材料关系；忽略并绝不复制 Image 1 的完整双页书、逐行卡片、顶部
指南针横条、黄铜边框、页脚大蜡封、飘带、按钮、分隔线、文字和整体布局。

Image 2 只提供当前游戏内任务纸张的综合色、左上暖光、磨损尺度和“墨迹贴在
纸面上”的接触感。不要复制 Image 2 的书壳、纸页轮廓、页沟、装订、包角或
任何全幅背景。最终四枚压印必须是独立物件，周围只有色键背景，不能带矩形
纸片或共同展示板。

输出恰好一张 `1024 × 1024` 图。画布为严格 `2 × 2` 等分布局，每格
`512 × 512`，不要画格线、边框、标题或标签。每个格子只允许一个居中的
压印，全部可见像素必须落在该格中央 `256 × 256` 安全盒内，四周保留宽阔、
均匀、互不相连的纯绿色隔离。四枚正面平视、无透视倾斜、无落地面、无长
投影，视觉面积、外轮廓重量和墨色强度相当。

格位和物件必须严格如下：

1. 左上 `elite`：一枚紧凑的三尖冠军纹章剪影。中尖略高，两侧尖较低，
   下方只有一个粗短承托折角；它像公会书记员用旧印章盖出的精锐标记，
   不是珠宝王冠、盾牌、头盔或字母。
2. 右上 `dungeon`：一座矮而厚的圆拱石门剪影，两侧各一块粗短门柱，
   中央只有一个大而清楚的门洞负形；不是城堡全景、钥匙孔、房屋或方形
   按钮。
3. 左下 `raid`：三面紧凑的竖向行军旗组成一个整体剪影，中旗略高，
   两侧旗略低，底部共用一个粗短基座；不是人物头像、三把剑、字母或
   复杂军徽。
4. 右下 `pvp`：两把短而宽的香草魔兽式战剑交叉成清楚的 X，剑身和护手
   都必须足够粗，只有一个中央交叉负形；不是细针、骷髅、阵营徽记或
   发光武器。

四枚使用同一种深乌棕旧墨，边缘有少量暖赭干刷缺口和纸面压印形成的极短
内侧明暗，但不能生成独立纸张底色、黄铜底座、彩色填充或外发光。每枚只保留
一个主剪影，最多一个大负形；禁止微型刻字、细密纹章、颗粒噪点和缩到
`10 × 10px` 后会消失的纤细线。外缘略有手工误差，但四枚不能变成四种
不同画风。

背景必须是完全均匀、无纹理、无渐变、无暗角的纯 `#00FF00`。压印本身及
边缘不得出现绿色、绿色反射或绿色描边；安全盒外不得有阴影、碎屑、辅助线
或额外物件。禁止现代扁平矢量 icon、移动应用 badge、细金线网页图标、
玻璃、霓虹、镜面黄金、暗黑 3 式尖刺黑铁、恶魔角、骷髅和照片级古董。

最终自检：必须恰好四枚且格序为 elite／dungeon／raid／pvp；无文字、无
底座、无格线；每枚完整落在各自中央安全盒；四种剪影在 `10 × 10px` 下仍
可区分；Image 1 的香草手绘线重与 Image 2 的纸面综合色得到继承；背景只有
均匀纯 `#00FF00`。

## 最终执行正文 — QL-B3-B V1

制作一枚《魔兽世界》60 级香草时代任务目录中的“限时任务沙漏压印”二维
手绘 UI sprite。它是盖在旧公会任务纸面上的小型计时档案墨印，只表达
`timed`；不是可点击按钮、倒计时面板、怀表、金属徽章或任务卡。倒计时数字、
任务名、等级和任何文字都由游戏运行时负责，图中绝不出现文字、字母或数字。

Image 1 是最高视觉权威。继承它的 2004 年前后香草魔兽二维手绘位图语言：
粗厚略不规则轮廓、有限且清楚的明暗切面、左上暖光、低饱和暖赭与烟褐色温、
真实旧墨和克制边缘磨损。只继承年代、笔触、线重、综合色和材料关系；
忽略 Image 1 的完整书体、逐行卡片、指南针、黄铜边框、页脚大蜡封、飘带、
按钮、分隔线、文字和完整布局。

Image 2 只提供当前任务纸张综合色、左上暖光、磨损尺度和墨迹贴纸面的接触感。
不要复制它的书壳、纸页外轮廓、中央装订、包角或背景。最终沙漏必须是一个
独立 sprite，周围只有色键背景，不带矩形纸片、圆形硬币或展示底座。

输出恰好一张 `1024 × 1024` 图，只允许一个正面平视、垂直朝上的沙漏压印。
全部可见像素必须落在画布中央 `320 × 320` 安全盒内，四周留出均匀纯绿色
隔离；无透视倾斜、无桌面、无长投影、无第二枚物件。

沙漏由上下两条厚而略弯的横梁、两侧两根短粗立柱和中央一个清楚的收腰玻璃
负形组成。上腔保留一小块深色“砂”，下腔保留一块稍大的深色“砂”，中间
只用一条粗短下落痕连接；结构要像旧公会压印里的沙漏符号，不是写实玻璃器皿。
所有结构必须用少量粗线和大负形完成，缩到 `10 × 10px` 仍先读出沙漏，
不能依赖细线、颗粒或精密齿轮。

使用与任务目录墨记同家族的深乌棕旧墨，允许极少量氧化暗金／暖赭干刷落在
左上凸缘，形成克制的压印明暗；不得成为立体黄铜摆件、发光警报或彩色宝石。
边缘略有手工误差和轻微缺墨，磨损只在横梁与立柱凸缘，不在中心堆积噪点。

背景必须完全均匀、无纹理、无渐变、无暗角，且只能是纯 `#00FF00`。沙漏
本身及边缘不得出现绿色、绿色反射或绿色描边；安全盒外不得有阴影、碎屑、
辅助线或额外图形。禁止文字、数字、方框、圆形底座、现代扁平 icon、移动
应用计时 badge、细金线、霓虹、玻璃 HUD、精密机械、镜面黄金、暗黑 3
尖刺黑铁、骷髅和照片级古董。

最终自检：画面恰好只有一枚垂直沙漏；完整落在中央安全盒；无文字、数字、
底座和额外物件；在 `10 × 10px` 下仍有清楚的上下横梁、收腰和砂流；
综合色、手绘线重和光向继承 Image 1／2；背景只有均匀纯 `#00FF00`。

## 最终执行正文 — QL-B3-C V1

制作一张只包含《魔兽世界》60 级香草时代任务目录“任务状态蜡封”的二维
手绘 UI sprite atlas。它表现同一枚旧公会蜡封的两个物理状态：
`complete` 完整、`failed` 破裂。它们是放在任务行末端的微型状态章，
不是按钮、硬币、奖励徽章、绿色勾或红色叉；任务名、完成文字和所有动态
内容仍由游戏运行时负责，图中绝不出现文字、字母、数字、勾号或叉号。

Image 1 是最高视觉权威。继承它的 2004 年前后香草魔兽二维手绘位图语言、
粗厚略不规则轮廓、有限而明确的明暗切面、左上暖光、低饱和暗酒红／烟褐／
暖赭综合色，以及页脚旧蜡封的材料厚度、压纹深度和边缘磨损。只继承年代、
笔触、综合色与蜡材质；绝不复制 Image 1 的完整书、页脚尺寸、蓝色飘带、
指南针星、黄铜框、逐行卡片、按钮、文字或整体布局。

Image 2 只提供当前任务纸面的综合色、左上暖光、磨损尺度和蜡封贴在纸面上的
短接触阴影。不要复制它的书壳、纸页轮廓、页沟、装订或包角。最终两枚状态
必须是彼此独立、可单独裁切的 sprite，周围只有色键背景，不带纸片或共同
展示板。

输出恰好一张 `1024 × 1024` 图，画布左右等分为两个 `512 × 1024` 格位，
不要画中线、格框、标题或标签。左格只放 `complete`，右格只放 `failed`。
每枚全部可见像素必须落在各自格位中央 `256 × 256` 安全盒内；两枚正面
平视、尺寸和中心一致，四周有宽阔纯绿色隔离，不得相连或互相投影。

两态必须明显来自同一枚小型暗酒红旧蜡封：近圆但略有手压误差的厚蜡外缘、
相同的八向公会罗盘星浅压纹、相同综合色、相同左上短高光和相同边缘磨损
位置。压纹必须粗、浅、简单，缩到 `12 × 12px` 时仍作为一个中央凹痕读出，
不能是精细珠宝纹章。

- 左侧 `complete`：蜡封完整、连续并贴合，圆缘只有自然小缺口；中央压纹
  完整，不能增加勾号、文字、绿光或胜利光环。
- 右侧 `failed`：以左侧同一蜡封为物理原型，沿左上到右下形成一条清楚的
  锯齿断裂，分成两块略微分离的残片；透明裂隙在缩到 `12 × 12px` 后仍至少
  读出一条窄缝。压纹在两块残片上连续错开，外径、蜡色、光向和磨损不能
  变成另一枚封印。不得增加叉号、火焰、碎石爆炸或散落小碎片。

蜡色以低饱和暗酒红和深棕阴影为主，左上只有短促暖赭高光；保持实体厚度，
但不能像照片级蜡章、塑料按钮、镜面宝石或金属硬币。接触阴影只能是贴着
下缘的极短深褐像素，不能延伸出安全盒。

背景必须完全均匀、无纹理、无渐变、无暗角，且只能是纯 `#00FF00`。蜡封
本身及边缘不得出现绿色、绿色反射或绿色描边；安全盒外不得有阴影、碎屑、
辅助线或额外物件。禁止文字、勾叉、飘带、按钮框、圆形金属底座、现代扁平
badge、霓虹、玻璃、镜面黄金、暗黑 3 尖刺黑铁、骷髅、恶魔角和照片级古董。

最终自检：必须恰好两枚，左 complete 完整、右 failed 为同一枚封印的两块
破裂残片；相同尺寸、压纹、综合色、光向和磨损；无文字、勾叉、飘带、底座
和额外碎片；每枚完整落在各自中央安全盒；在 `12 × 12px` 下完整与破裂仍
可辨；背景只有均匀纯 `#00FF00`。

## QL-B3-A V1.r1 完整修复正文

编辑 Image 3，制作一张只包含四枚《魔兽世界》60 级香草时代任务目录
“类型压印”的二维手绘 UI sprite atlas。四枚仍然是盖在旧公会任务纸面上的
小型档案墨印，不是按钮、硬币、金属徽章、任务卡片或独立面板；图中绝不
出现任务名、等级、`Elite`、`Dungeon`、`Raid`、`PvP` 等任何文字、字母
或数字。

Image 1 是最高视觉权威。继承其 2004 年前后香草魔兽二维手绘位图语言：
粗厚略不规则的手工轮廓、有限而明确的明暗切面、左上暖光、低饱和暖赭与
烟褐综合色、真实旧墨和克制磨损；忽略完整双页书、逐行卡片、指南针横条、
黄铜边框、页脚蜡封、飘带、按钮、分隔线、文字和整体布局。Image 2 只提供
当前任务纸张综合色、左上暖光、磨损尺度和墨迹贴纸面的接触感；不要复制
书壳、纸页轮廓、页沟、装订、包角或全幅背景。

Image 3 是本段前一次生成的四格候选，也是本次唯一编辑目标。必须完整保留
Image 3 中已经正确的四个物件身份、格序、正面视角、主剪影、深乌棕墨色、
暖赭干刷、手绘边缘、磨损位置和内部大负形：左上三尖冠军纹章、右上圆拱
石门、左下三面竖向行军旗、右下两把交叉短剑。不要重新设计、替换、旋转、
镜像、交换或增加任何物件。只允许对每格中的现有物件做等比缩小和二维平移，
并把背景清理为严格纯色。

输出恰好一张 `1024 × 1024` 图，严格 `2 × 2` 等分，每格
`512 × 512`，不要格线、边框、标题或标签。四格中心依次为
`(256,256)`、`(768,256)`、`(256,768)`、`(768,768)`。每个现有物件都要
等比缩小并精确居中：其可见像素包围盒宽和高都不得超过 `220px`，且必须
完整落在该格中央 `256 × 256` 安全盒内；四周必须保留宽阔、均匀、互不
相连的色键隔离。不得拉伸宽高比，不得裁掉尖端、门柱、旗角、剑尖、护手
或磨损边缘。

格序和语义保持不变：

1. 左上 `elite`：Image 3 的三尖冠军纹章，中尖略高、两侧尖较低、下方
   粗短承托折角；不是珠宝王冠、盾牌、头盔或字母。
2. 右上 `dungeon`：Image 3 的矮厚圆拱石门，两侧粗短门柱和一个清楚的大
   门洞负形；不是城堡全景、钥匙孔、房屋或按钮。
3. 左下 `raid`：Image 3 的三面紧凑竖向行军旗，中旗略高、两侧略低并共用
   粗短基座；不是人物、三把剑、字母或复杂军徽。
4. 右下 `pvp`：Image 3 的两把短宽战剑交叉成 X，保留粗剑身、护手和中央
   交叉负形；不是细针、骷髅、阵营徽记或发光武器。

四枚继续使用同一种深乌棕旧墨、少量暖赭干刷缺口和极短压印明暗；不能增加
纸张底色、黄铜底座、彩色填充、外发光、微型刻字、细密纹章或新噪点。缩到
`10 × 10px` 后必须仍以粗主剪影区分四类，且四枚视觉面积和墨色强度相当。

整张背景必须重新清理为完全均匀、无纹理、无渐变、无暗角的精确
`#00FF00`。物件本身及边缘不得出现绿色、绿色反射或绿色描边；每格安全盒外
不得有阴影、碎屑、辅助线或额外物件。禁止现代扁平矢量 icon、移动应用
badge、细金线网页图标、玻璃、霓虹、镜面黄金、暗黑 3 式尖刺黑铁、
恶魔角、骷髅和照片级古董。

最终自检：恰好四枚，格序仍为 elite／dungeon／raid／pvp；Image 3 的四个
物件与美术细节保持，唯一结构变化是等比缩小和居中；每个 bbox 的宽高均
不超过 `220px` 并完整落入中央 `256 × 256` 安全盒；无文字、底座、格线
和新物件；`10 × 10px` 下四类可辨；背景只有均匀精确 `#00FF00`。

## 自主修复循环

### 不可变修复边界

- 三段组件 ID、对象／状态数量、格序与同时显示语义。
- Image 1／2 的固定 SHA、权威顺序与 inherit／ignore 职责。
- 三个 `1024 × 1024` source 画布、格位、安全盒、runtime
  `10px`／`10px`／`12px` 尺寸和三槽位置。
- 纯 `#00FF00` 色键、无文字、无底座、无新增命中区和动态内容不烘焙。
- A／B／C 是三个独立预算，不能把某段剩余额度借给另一段。

### 允许的自主修复

- 每段可在其冻结物件身份内加强剪影、线重、同源关系、综合色、磨损、
  安全盒占用、纯色键和小尺寸可读性的措辞。
- 可根据失败门禁选择 fresh generate 或 scoped edit。
- 允许把同一段循环前一次输出作为该段下一次 edit 的额外输入；不得跨段
  复用输出，也不得上传 QL-B1／QL-B2 候选。
- 每个 countable output 后必须先完成 Alpha／结构检查与真实排版预演；
  内审全部通过即提前停止该段。

### 必须重新授权

- 新增或删除对象／状态、改变格序、画布、安全盒、runtime 尺寸、三槽位置、
  色键策略、材料隐喻或最高视觉方向；
- 新增外部 Image 3（不含已授权的同段前次输出）或其他外部输入、跨段 edit、
  改用其他 provider；
- 把类型章改成金属徽章、把状态章改成勾叉，或用单槽优先级替代三槽并存。

### 实际生图记录

| 段 | 实际生图 | 正文版本／执行前 commit | 操作 | session／result | 输出／SHA | 第一失败门禁 | 保留区域与下一步 | 结论 |
|---|---:|---|---|---|---|---|---|---|
| A | 1/5 | `QL-B3-A V1` / `265503a` | generate | `019fb293-2175-7070-a257-87086887f603`／`ig_0cb39047645036d8016a6b27c524288191801b7e6dd6d8a610` | native `6bb9996a…`；normalized `1af5e8b3…`；transparent `011c7eda…`；layout `1586abe8…` | 四格可见 bbox 全部越出中央 `256 × 256` 安全盒；native 色键为 `#04F904` 而非精确 `#00FF00` | 保留四个正确物件、格序、正视、综合色与 10px 可读性；以本段输出作为 Image 3，只做等比缩小、居中和纯色背景清理 | internal-fail；`1/5` |
| A | 2/5 | `QL-B3-A V1.r1` / 待提交 | edit |  |  |  |  |  |
| B | 1/5 | `QL-B3-B V1` / `265503a` | generate |  |  |  |  |  |
| C | 1/5 | `QL-B3-C V1` / `265503a` | generate |  |  |  |  |  |

| 流程错误 | 段／正文版本／commit | session | 错误与无生成证据 | 针对性修复 | 结论 |
|---:|---|---|---|---|---|
| E1 | A／`QL-B3-A V1`／`923c6f0` | local fixed-executor launch | `npx` 请求 npm registry 时返回 `EPERM`；无输出文件、无 provider result、无生成证据 | 保持正文与 Image 1／2 不变，改在获准网络／用户 npm cache 环境重试 | 不占生图额度 |
| E2 | A／`QL-B3-A V1`／`3dfc92e` | fixed CLI argument transport | 固定 CLI 返回 `Reading prompt from stdin / No prompt provided via stdin`；无输出文件、无 provider result、无生成证据 | `codex exec -i <FILE>...` 的 image 参数为 variadic；保持正文与输入不变，在两张 image 参数后增加 `--` 终止选项并传入同一 prompt | 不占生图额度 |
| E3 | A／`QL-B3-A V1`／`b00918b` | `019fb290-ca5b-7b02-a8f4-d014bb24c303` | 子进程从仓库工作目录再次读取包装 skill，准备递归启动固定 CLI；主流程在任何 `image_gen` tool call／provider result 前中断，无输出文件 | 按仓库 skill 改用独立临时工作目录、`-s workspace-write`，并明确当前 0.143.0 子进程只调用自身 built-in `image_gen`、不得读取包装器或启动 `codex`／`npx` | 不占生图额度 |

## 执行记录

- 用户授权范围已经冻结（commit `265503a`）；流程错误 E1／E2／E3 均无
  provider 生成证据，不占实际生图额度。

### QL-B3-A V1 attempt 1

- 日期：`2026-07-30`。
- 固定执行器：`@openai/codex@0.143.0`，子进程 session
  `019fb293-2175-7070-a257-87086887f603`，provider image
  `ig_0cb39047645036d8016a6b27c524288191801b7e6dd6d8a610`。
- 输入：固定 Image 1 SHA `03dc589a…`；固定 Image 2 SHA `91f9feca…`。
- executor user block 已核对为完整 `QL-B3-A V1`；没有报告 revised prompt。
- native：`1254 × 1254 RGB`，
  `generated/quests/QL-B3/v1/type/attempt-01/raw/QL-B3-A_V1.raw.png`，
  SHA-256
  `6bb9996a8b8f83b9185c0d4e1c123f74ce9de0077bec4e2722fc456a5b945364`。
- 确定性归一化：`1024 × 1024 RGB`，SHA-256
  `1af5e8b37c0104175cf34158a8ebc00eab7ee68ccce8c3377c7cd77b3a48563e`。
- 固定色键清理使用 corner key `#04F904`；透明候选 SHA-256
  `011c7eda3751aff17b148fa433b8c39b764faa6cf327d3fd8857da054ea079d1`，
  Alpha `864652` 全透明／`7677` 半透明／`176247` 不透明，可见强绿 `0`。
- `676 × 464`、23 行真实排版：
  `generated/quests/QL-B3/v1/type/attempt-01/previews/real_layout_676x464.png`，
  SHA-256
  `1586abe81346c5a163cab3a5eb2d33c24db11c6cb5d57a3efd1dacabd54d412a`；
  当前 A 为权威候选，B／C 使用右页明确标注的中性非权威占位。
- 实际生图：A `1/5`；流程错误累计 `3`；无 tracked source／runtime。

## 审查记录

- A attempt 1 结论：`internal-fail / repair-prepared / P3`。
- 语义／物理：pass。恰好四枚，顺序和身份分别为三尖冠军纹章、圆拱石门、
  三面行军旗与交叉短剑；无文字、格线、底座或额外对象，全部正面平视。
- 美术继承：pass。四枚共享深乌棕旧墨、暖赭干刷、左上暖光和同一手绘线重；
  没有现代玻璃、霓虹、金属卡片或暗黑 3 尖刺语言。
- 小尺寸与真实排版：pass。确定性 runtime 缩放后四类在 `10px` 下仍可
  区分，全部 23 行、`155px` 标题安全区和四槽最高密度未发生状态重叠；
  B／C 灰色图形只是不参与美术判断的非权威占位。
- 第一失败门禁：source 安全盒。elite bbox `143,127,399,416`；
  dungeon `76,117,388,405`；raid `119,48,430,358`；pvp
  `84,74,375,357`，四格均未完整落入本地 `128,128,384,384`。
- 次要失败：native 背景采样为 `#04F904`，不是执行正文要求的精确纯
  `#00FF00`；确定性 Alpha 已无可见绿，但不能把 raw 失败改写成通过。
- 修复决定：scoped edit。V1.r1 只允许保留 Image 3 的四物件并等比缩小、
  居中、清理背景；不改变格序、物件身份、材料、画布或 runtime 合同。
- 任一段内部通过只到 `candidate-reviewed / P3`，仍需用户接受具体候选才能
  晋级 `assets/source/`。
- runtime 导出前还必须冻结显式 `questTag` 等值表；未确认 token 只影响
  类型章显示，不阻塞 B／C 的 source 生产。

## 尝试摘要

| 版本 | 执行／审查证据 | 结论 | 下一版必须改变 |
|---|---|---|---|
| QL-B3-A V1 | session `019fb293-2175-7070-a257-87086887f603`；transparent `011c7eda…`；layout `1586abe8…` | internal-fail；`1/5` | 保持物件与美术；四格 bbox 等比缩入安全盒并清理 native 色键 |
| QL-B3-B／C V1 | 用户已授权；实际生图各 `0` | prompt-authorized | 执行各自 attempt 1 |

## 下一门禁

提交 A attempt 1 审查、完整 `QL-B3-A V1.r1` 正文和预演工具后，以固定
Image 1／2 加同段 Image 3 执行 A attempt 2；B／C 保持各 `0/5`。
