# 乾坤袋当前进度

- Provider：客户端独立安装的 Bagshui 1.5.15；AEUI 不修改其源码或 SavedVariables。
- runtime 1.3 / P5：背包、银行、钥匙链主框共用皮革护角与烟褐布料；搜索框内衬、
  普通分组低对比边线与工具栏皮革底座已接入。主框边缘外扩 10 UI，保留原始内容净空。
- SatchelDonorV1 已接受用于实现；512² 主框 atlas 与 128² 可平铺布料，采样至少 2×。
- 用户截图确认恢复原生标题后显示正常；用户确认搜索和选择正常。布料直接调暗，不使用覆盖层。
- 品质边默认使用内缩 2 UI、宽 1 UI 的低饱和染色线；普通品质为灰褐色，稀有品质保留色相。
  原生品质着色转译到内沿，透明度原样保留；容器提示等非品质颜色继续使用原边框。
- 物品槽与底部袋位接入 Character 皮革边沿、悬停及按下纹理，保持真实图标、
  数量、冷却、品质边、容器选中与搜索淡化；工具栏复用 Gear Planner 材质。
- 品质边及全部背包外观为 Overhaul 全角色默认，不创建角色外观配置；
  `/aeui bags` 使用账号共用开关并即时还原。分类、尺寸、右侧对齐、
  搜索、拖放、品质色、冷却与离线角色数据由 Bagshui 持有。
- 编辑模式继续使用原生颜色提示；离线角色库存保留红色边缘语义。
- 格间距使用全角色统一紧凑布局：布局计算期间额外 itemMargin 为 0，保留 provider
  边框净空与图标尺寸，不写入 Bagshui 配置；禁用适配后重新布局恢复原间距。
- 待实机：`/reload` 后检查槽位边沿、悬停／按下、搜索淡化、底部袋位选择；相邻验证银行／钥匙链、离线角色、
  编辑模式拖放；使用 `/aeui bags` 检查还原。尚未标记 P6。
