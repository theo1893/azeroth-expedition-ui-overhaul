# 生产专业进度

## 当前状态

`2.0 / P5`：已接入用户确认的 WORKBENCH V2 专用媒体。保持 `760×548`
双栏与 22 行目录；木质外缘固定密度分段铺设，标题在木边下独立居中，目录／按钮／输入框
采用独立小尺寸切片，滚动滑块／箭头／关闭字形分别导出。文本和图标不烘焙。
已加载 AEUI 时使用新材质；未加载时保留 pfUI 底色。新增媒体需完整重启客户端。
复用 `CraftFrame` 的猎人训练继续保留其原生操作与显隐。

运行时入口：`addon/pfUI/skins/blizzard/professions.lua`，同时登记独立的
`profession-queue` 功能模块，避免额外运行时文件未加载导致按钮和原料汇总缺失。
Aux 成本接口未变。pfUI 独立 `profession-queue` 模块新增“连续制作”按钮，
使用数量输入框中的制作次数；预检当前专业已学配方和背包库存，递归补做缺少的
中间材料，再制作目标。已有材料优先使用，产出按配方最低数量保守规划。
所选配方详情底部显示“基础原材料”：按输入制作次数展开依赖、扣除现有中间材料，
汇总基础原料需求／背包数量／缺口，并随选中、数量和背包变化刷新；长清单纳入详情滚动。
不跨专业、不购买、不计银行库存，不接管附魔目标选择。“制作”按输入次数规划依赖；“全部制作”按当前基础库存计算可完成的最多次数。
同配方原生批量制作；跨配方自动选中下一项并将制作按钮改为“继续制造”，需要真实点击后才启动。
客户端 DoTradeSkill 的输入事件限制不允许从定时器自动启动不同配方；等待点击不发请求、不超时。两按钮的可用状态按完整材料链判断，执行中不能重复开批。
单批最多 1000 次（含中间材料制作）；原有连续制作按钮仍可停止队列。
关闭／切换专业、制作失败、打断或点击停止会终止队列；同一种中间材料使用原生批量制作，整批确认原料扣除与产物入包，等待实际施法空闲与网络延迟余量后进入下一配方的点击等待；不依赖单一施法结束事件。
运行时临时展开分类、放开类别／部位过滤，结束时尽力恢复原过滤、折叠与目标选择。

## 待实机验证

- 完整重启后检查木边接缝、角料、居中标题、下拉／搜索、滚动轨／滑块和按钮状态；工程与附魔共用。

- 连续制作：A 缺 B 但原料齐全、已有部分 B、多层依赖、共享原料、数量大于一；核对实际消耗与最终产出。
- 原料不足应在制作前说明；移动打断、缺工具、背包满、关闭窗口、点击停止不能继续队列。

- 附魔与任一普通生产专业：搜索、两种筛选、类别／部位下拉、目录折叠与滚动。
- 详情长文本／工具需求／冷却，材料不足灰态、八材料网格、材料提示和 Aux 成本。
- 普通专业数量输入／加减／全部制作／制作／退出，切换专业后的标题、技能值与列表刷新。
- 相邻回归：物品提示框；如使用猎人训练，确认训练点、需求与训练按钮。

## 回退

pfUI 设置 → Components／组件 → Skins／皮肤 → Disable Skin Profession，
重载后交回原生专业窗口。禁用 AEUI 后仅移除远征材质依赖；pfUI 仍持有布局。
独立替代生产插件不在当前接管范围。禁用 pfUI 模块 `profession-queue` 并重载可独立移除连续制作功能。
