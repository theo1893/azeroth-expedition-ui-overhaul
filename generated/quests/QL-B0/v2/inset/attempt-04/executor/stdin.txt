$imagegen 使用 `@openai/codex@0.143.0` 的固定 ImageGen 执行器，生成一张
`1024 × 1024` RGBA 位图；整张画布也是唯一的 `1024 × 1024` 归一化设计
网格，不得裁边。若 provider 使用不同的正方形原生边长，仍必须严格保持下面
坐标在完整画布中的比例，以便后续只做一次全画布等比归一化。所有不属于物件
的像素，包括外部背景和中央开口，都必须是完全均匀、无纹理、无渐变、无阴影、
无光晕、无色偏的精确 RGB `#00FF00`。画面中只允许有一个正面朝向、无透视
的 Quest Log 左页目录内框 `QUEST.LOG.LIST.INSET`。

输入角色必须严格遵守。Image 1
`任务详情面板_视觉基准_v1.png`
（SHA-256
`03dc589abad7187c478ec484cc6565f2c16d2ce52d2d6421251a4de6437453bd`）
是最高视觉权威：继承正式公会任务卷宗身份、2004 年前后香草魔兽二维手绘
笔触、略不规则但清楚的明暗切面、左上暖光、低饱和赭黄／烟黑／暗酒红与
氧化黄铜关系，以及被长期翻阅造成的不均匀磨损。不要复制 Image 1 的完整
书体、顶部控件、逐行内容、文字、图标、按钮、滚动条、封印或书签。

Image 2 `QuestLogBookShell_Master_v1.png`
（SHA-256
`91f9fece41ed375df1fa32e94b18797cbb280c0b5e99478862473589c671edd5`）
只用于匹配当前 runtime 的纸页接触色、材质尺度和受光连续性。不要复制完整
书壳、书脊、四角、页沟或纸页；Image 2 不得覆盖 Image 1 的物件身份、
轮廓语言和笔触。

构图和厚度是本次最高几何门禁。唯一物件以 `(512, 512)` 为中心。它不是
常见的瘦高肖像框：外框宽高比必须为 `524:680 = 0.7706`，高度只等于宽度
的 `1.298` 倍。可见外接盒严格位于
`x=250..774, y=172..852`，恰为 `524 × 680px`；也就是画布宽度的
`51.17%`、高度的 `66.41%`。左、右各留 `250px` 纯绿，上、下各留
`172px` 纯绿。盒外不得有任何物件像素、投影或光晕。

中央开口严格位于 `x=266..758, y=188..836`，恰为 `492 × 648px`；
开口宽高比为 `0.7593`。外框与开口之间只允许一层结构，在每一段直边上，
从最外可见像素到纯绿开口的距离都不得超过 `16px`。上、下、左、右的可见
材料带均为 `16px`，禁止第二圈内框、暗色衬框、阴影边、凸起边或任何额外
平行轮廓。

整张画布必须让人第一眼看到“大面积空纯绿 + 一圈很细的小型装订边”。
理论可见材料面积约为
`524 × 680 - 492 × 648 = 37,504px`，只占整张画布约 `3.58%`；
外部背景与中央开口合计至少约占 `96.4%`。不得生成占画布约一成的常规厚
边框。缩到 `262 × 340 UI px` 后，材料只能形成四边各 `8px` 的嵌边，
必须完整露出 `246 × 324 UI px` 的十八行阅读区。

这个物件是嵌在暖赭纸页接缝处的“薄目录装订唇边”，不是完整书壳、窗框、
门框、肖像框或独立厚面板。主体只用深胡桃木色旧皮与烟黑硬边；氧化黄铜只可
作为两个小而不同的接缝夹片：一个靠近左上角，一个靠近右下角；另外两个角
不放黄铜。每个夹片都必须完全落在 `16 × 16px` 角部薄带内，不能成为包角、
三角大金属片、铆钉阵列或连续金属边。禁止四角相同或对角镜像。禁止把浅色
羊皮纸、砂岩、石板或厚木条当作四边主体。四边可有轻微手工起伏、缺口和
不均匀磨损，但必须始终留在 `16px` 薄带中；装饰密度不能规律重复。

只保留低频、概括的二维手绘明暗，不要连续高光线、深黑内阴影、均匀压花、
高清裂纹或照片级皮革。不要为了展示材质而加宽任何一边。轮廓要能看出手工
切边，但不能弯成波浪或破坏矩形开口。整体应像 Image 1 书页上的小型目录
装订细节，而不是单独展示的豪华奇幻边框。

禁止绘制任何纸面填充、行分隔、地区条、任务条、文字、数字、图标、按钮、
滚动条、滑块、选择高亮、书签、印章、状态章、假肖像槽或假图标槽。禁止现代
矩形面板、暗黑祭坛、聊天旧书风格、细金框、宝石、巨大徽章、浮夸雕花和常亮
发光。

输出前逐项自检：只有一个单层薄目录内框；外框宽高比为 `0.7706`，不是
瘦高框；可见材料约占画布 `3.58%`，绿色约占 `96.4%`；中心、外盒、开口
和每条直边最多 `16px` 厚度全部正确；只有左上与右下两个不同的小黄铜接缝，
且都在薄带内；外盒以外与开口以内都是同一个精确 `#00FF00`；没有第二圈
内框、投影、绿色纹理或抗锯齿污染扩散到安全区；内框正面、无透视、无拉伸；
在 `262 × 340 UI px` 显示时不会遮挡 `246 × 324 UI px` 的十八行内容；
没有任何被禁止的动态或交互内容。

Execution instruction: Image 1 is D:\Git\azeroth-expedition-ui-overhaul\assets\locked\quests\任务详情面板_视觉基准_v1.png. Image 2 is D:\Git\azeroth-expedition-ui-overhaul\assets\source\quests\ql-a1\QuestLogBookShell_Master_v1.png. This Codex process is already @openai/codex@0.143.0. Fulfill the $imagegen request inside this process with its built-in image_gen tool. Do not read or invoke the imagegen-0-143-0 wrapper skill, and do not start any codex or npx subprocess. Save the provider output unchanged to ./generated and output its absolute file path directly. Do not review, resize, crop, recolor, chroma-key, or otherwise post-process it.