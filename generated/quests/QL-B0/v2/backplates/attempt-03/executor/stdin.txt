$imagegen 使用 `@openai/codex@0.143.0` 的固定 ImageGen 执行器，生成一张
`1024 × 1024` RGBA 位图；这也是唯一的归一化设计网格。先把整张画布填成
完全均匀、无纹理、无渐变、无阴影、无抗锯齿污染的纯绿色 `#00FF00`，
再只在两个固定矩形盒内绘画。画面中恰好有两个互不接触、正面朝向、无透视
的超宽平面目录底条；盒外不得有任何物件像素、磨损、投影或光晕。不要生成
完整 UI、书页、书框、状态展示板、金属牌匾、独立卡片或按钮。

输入角色必须严格遵守。Image 1
`任务详情面板_视觉基准_v1.png`
（SHA-256
`03dc589abad7187c478ec484cc6565f2c16d2ce52d2d6421251a4de6437453bd`）
是最高视觉权威：继承正式公会任务卷宗身份、2004 年前后香草魔兽二维手绘
笔触、粗厚略不规则轮廓、明确明暗切面、左上暖光、低饱和赭黄／烟黑／暗酒红
与氧化黄铜关系，以及不均匀的翻阅磨损。不要复制 Image 1 的完整书体、顶部
控件、文字、图标、肖像槽、按钮、滚动条、封印、书签或完整列表。
Image 1 中的黄铜只定义整套卷宗的综合色关系，本批两个细薄目录底条不得
绘制任何黄铜物件、端帽、包角、徽记、铆钉或几何装饰。

Image 2 `QuestLogBookShell_Master_v1.png`
（SHA-256
`91f9fece41ed375df1fa32e94b18797cbb280c0b5e99478862473589c671edd5`）
只用于匹配当前 runtime 的暖赭纸页接触色、材质尺度和左上受光连续性。
不要复制完整书壳、书脊、四角、页沟或纸页；Image 2 不得覆盖 Image 1 的
物件身份、轮廓语言和笔触。

上方唯一物件是 `QUEST.LOG.REGION.BACKPLATE`。中心必须正好为
`(512,304)`；唯一可见外接盒必须严格位于
`x=112..912, y=272..336`，恰为 `800 × 64px`、`12.5:1`。它只占画布
宽度的 `78.125%` 和高度的 `6.25%`；左、右各有 `112px` 纯绿，上方
有 `272px` 纯绿，盒底到下一物件之间有 `352px` 纯绿。绝不能把它扩成
约九成画布宽、约一成画布高的横幅。

地区条是贴在连续纸页上的低饱和暗橄榄涂色厚纸／极薄旧皮，正面、平坦、
低对比。四边只允许一笔不连续的烟黑手绘墨边；设计网格中上、下视觉边厚
各不超过 `3px`，左右端各不超过 `3px`，没有第二层边、内框、浮雕、
连续高光或投影。中央至少 `780 × 54px` 保持安静，专门承载动态地区文字。
左右端只能自然、略不相同地收断；不得出现菱形、圆形、三角形、徽章、端帽、
夹片、铆钉、扣具、金属轨道、闭合金框或任何可被误读为图标／按钮的形状。

下方唯一物件是 `QUEST.LOG.ROW.BACKPLATE`。中心必须正好为
`(512,720)`；唯一可见外接盒必须严格位于
`x=112..912, y=688..752`，同样恰为 `800 × 64px`、`12.5:1`。它也只占
画布宽度的 `78.125%` 和高度的 `6.25%`；左、右各有 `112px` 纯绿，
盒底以下有 `272px` 纯绿。绝不能把它扩成约九成画布宽、约一成画布高。

任务条是更浅、更薄的暖赭卷宗笔洗／薄纸条，仍贴在同一连续纸页上。四边
只允许一笔不完整的深棕墨线；设计网格中上、下视觉边厚各不超过 `2px`，
左右端各不超过 `2px`，中央至少 `784 × 56px` 保持平静。只允许少量
低频、不对称的自然缺口与旧化；不得出现内框、浮雕、连续高光、压花纹样、
金属边或实体厚度。左右端只能自然收断，不能出现任何几何符号、装饰端帽、
徽记、包角或假图标。

这里只生成两个 normal base，不绘制 `normal／hover／pressed／disabled`
四态，不绘制状态矩阵。禁止绘制文字、数字、等级、任务计数、箭头、勾选圈、
书签、选择高亮、类型章、计时章、完成／失败章、按钮、滚动条、肖像槽、
假图标槽或任何命中区。禁止两端菱形、十字、珠宝形状、厚金属包边、双层
闭合边框和整齐牌匾轮廓。禁止现代 UI 卡片、照片级皮革、完美镜像角、
细金框、高频雕花、宝石与常亮发光。磨损必须低频且不对称，但不能借磨损
越过固定 bbox。

输出前逐项自检：恰好两个独立横条；上方唯一可见 bbox 正好是
`[112,272]..[912,336]`，下方唯一可见 bbox 正好是
`[112,688]..[912,752]`，两者都是 `800 × 64px`、`12.5:1`；bbox 外与
两物件之间全部是同一个精确 `#00FF00`；左右纯绿边距各为画布
`10.9375%`，每条高度只为画布 `6.25%`；地区条边厚不超过 `3px`，
任务条边厚不超过 `2px`；没有菱形、徽记、端帽、铆钉、金属轨道或闭合
金属框；两个物件正面、无透视；缩到各自 `224 × 18 UI px` 时，暗橄榄
地区条与暖赭任务条仍可区分，但不会把十八行目录变成十八块厚牌匾；没有
任何被禁止的动态、状态或交互内容。

Execution instruction: Image 1 is D:\Git\azeroth-expedition-ui-overhaul\assets\locked\quests\任务详情面板_视觉基准_v1.png. Image 2 is D:\Git\azeroth-expedition-ui-overhaul\assets\source\quests\ql-a1\QuestLogBookShell_Master_v1.png. This Codex process is already @openai/codex@0.143.0. Fulfill the $imagegen request inside this process with its built-in image_gen tool. Do not read or invoke the imagegen-0-143-0 wrapper skill, and do not start any codex or npx subprocess. Save the provider output unchanged to ./generated and output its absolute file path directly. Do not review, resize, crop, recolor, chroma-key, or otherwise post-process it.