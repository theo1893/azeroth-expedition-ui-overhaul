$imagegen 使用 `@openai/codex@0.143.0` 的固定 ImageGen 编辑器。只编辑 Image 3，
Image 1 与 Image 2 只作美术参照；不要把 Image 1 或 Image 2 当作待编辑
画布。输出必须保持 Image 3 的完整 `1254 × 1254` 正方形画布，不裁边、
不改变画布长宽、不新增画布。该原生画布稍后只会等比归一化成
`1024 × 1024` source 设计网格，因此下面同时给出的原生像素盒与归一化盒
描述的是同一位置；原生像素盒是本次编辑的第一几何优先级。

输入角色必须严格遵守。Image 1
`任务详情面板_视觉基准_v1.png`
（SHA-256
`03dc589abad7187c478ec484cc6565f2c16d2ce52d2d6421251a4de6437453bd`）
是最高视觉权威：继承正式公会任务卷宗身份、2004 年前后香草魔兽二维手绘
笔触、粗厚略不规则轮廓、明确明暗切面、左上暖光、低饱和赭黄／烟黑／暗酒红
与氧化黄铜关系，以及不均匀的翻阅磨损。不要复制 Image 1 的完整书体、顶部
控件、文字、图标、肖像槽、按钮、滚动条、封印、书签或完整列表。
Image 1 中的黄铜只定义整套卷宗的综合色关系，本批两个细薄目录底条不得
绘制任何黄铜物件、端帽、包角、徽记、铆钉或几何装饰。

Image 2 `QuestLogBookShell_Master_v1.png`
（SHA-256
`91f9fece41ed375df1fa32e94b18797cbb280c0b5e99478862473589c671edd5`）
只用于匹配当前 runtime 的暖赭纸页接触色、材质尺度和左上受光连续性。
不要复制完整书壳、书脊、四角、页沟或纸页；Image 2 不得覆盖 Image 1 的
物件身份、轮廓语言和笔触。

Image 3 `QL-B0-B_V2_r2_attempt-03_raw.png`
（SHA-256
`5ee6ba57d94006b0833bba1e96074d3ebeab8d3d8e7b8f233ebde3d664030721`）
是唯一编辑目标。只继承 Image 3 中恰好两个正面、无端件、无金属框的平面
横条：上方低饱和暗橄榄涂色厚纸／极薄旧皮，下方更浅的暖赭笔洗／薄纸；
保留两者现有的低频表面色差、自然不对称收边和上暗下浅关系。不要继承
Image 3 的横条尺寸、纵向位置或近绿色背景；Image 3 不能覆盖 Image 1 的
最高美术权威。

这是最后一次、纯二维、像素坐标优先的几何修复，不是自由重绘。Image 3
原生 `1254 × 1254` 中，上条当前可见 bbox 约为
`[85,388]..[1168,516]`、`1083 × 128px`，下条当前可见 bbox 约为
`[85,766]..[1168,894]`、`1083 × 128px`。先分别把这两个现有对象从
背景中分离，再对每个对象做非等比二维缩放与平移；不要根据“构图平衡”
自行增加两条之间的距离，也不要根据纹理展示需要保留原高度。

上条 `QUEST.LOG.REGION.BACKPLATE` 的原生目标盒必须是
`[137,333]..[1117,411]`，宽 `980px`、高 `78px`，中心
`(627,372)`。也就是相对 Image 3 当前上条，把宽度缩为约 `90.49%`，
把高度缩为约 `60.94%`，中心横向约移 `+0.5px`、纵向移 `-80px`。
这个原生目标盒等比归一化后必须成为设计网格
`[112,272]..[912,336]`、`800 × 64px`。只保留现有暗橄榄平面纸／
极薄旧皮表面和自然边缘，不增加细节；归一化设计网格中的视觉边厚不得超过
`3px`，中央至少 `780 × 54px` 安静。

下条 `QUEST.LOG.ROW.BACKPLATE` 的原生目标盒必须是
`[137,843]..[1117,921]`，宽 `980px`、高 `78px`，中心
`(627,882)`。也就是相对 Image 3 当前下条，把宽度缩为约 `90.49%`，
把高度缩为约 `60.94%`，中心横向约移 `+0.5px`、纵向移 `+52px`。
这个原生目标盒等比归一化后必须成为设计网格
`[112,688]..[912,752]`、`800 × 64px`。只保留现有暖赭平面笔洗／薄纸
表面和自然边缘，不增加细节；归一化设计网格中的视觉边厚不得超过 `2px`，
中央至少 `784 × 56px` 安静。

不要重复 V2.r3 的过度分离错误。上一次未作为本次输入的结果仅提供负面
测量证据：它把上条放到原生约 `[144,289]..[1110,394]`、中心
`y≈341.5`，把下条放到约 `[144,859]..[1110,967]`、中心 `y≈913`，
并把两条保留成约 `105–108px` 高。那些位置和高度都是错误的。本次上条
中心必须是 `y=372` 而不是 `341.5`，下条中心必须是 `y=882` 而不是
`913`；两条都必须压薄到 `78px`，不得仍为 `100px` 以上。不要把
“向上／向下移动”理解成尽量拉开，只执行上面给出的最终绝对坐标。

几何编辑完成后，彻底清除两个旧 bbox 的残影。除两个原生目标盒以外，
完整 `1254 × 1254` 画布的每一个像素都替换为完全均匀、无纹理、无渐变、
无阴影、无光晕、无色偏的精确 RGB `#00FF00`。不要保留近绿色
`#0FF508`、绿色噪声或旧位置残影。允许只在物件轮廓内侧做抗锯齿；不得把
抗锯齿、投影或绿色溢色扩散到目标盒之外。两个物件不得接触，不得新增第三个
物件，也不得重新绘制新的横幅。

这里只生成两个 normal base，不绘制 `normal／hover／pressed／disabled`
四态，不绘制状态矩阵。禁止绘制文字、数字、等级、任务计数、箭头、勾选圈、
书签、选择高亮、类型章、计时章、完成／失败章、按钮、滚动条、肖像槽、
假图标槽或任何命中区。禁止两端菱形、十字、珠宝形状、厚金属包边、双层
闭合边框和整齐牌匾轮廓。禁止现代 UI 卡片、照片级皮革、完美镜像角、
细金框、高频雕花、宝石与常亮发光。磨损必须低频且不对称，但不能借磨损
越过固定 bbox。

输出前按原生像素逐项自检：完整画布仍是 `1254 × 1254`；恰好两个独立
横条；上方唯一可见 bbox 正好是 `[137,333]..[1117,411]`，下方唯一
可见 bbox 正好是 `[137,843]..[1117,921]`；两条都是 `980 × 78px`；
上条中心 `(627,372)`，下条中心 `(627,882)`；两条没有被过度向外移动；
归一化后分别落入 `[112,272]..[912,336]` 和
`[112,688]..[912,752]`；bbox 外与两物件之间全部是同一个精确
`#00FF00`，旧位置没有残影；两条确实是对 Image 3 正确平面物件的压薄、
缩宽与移动，不是新画的横幅；没有菱形、徽记、端帽、铆钉、金属轨道或
闭合金属框；缩到各自 `224 × 18 UI px` 时，暗橄榄地区条与暖赭任务条
仍可区分，但不会把十八行目录变成十八块厚牌匾；没有任何被禁止的动态、
状态或交互内容。

Execution instruction: Image 1 is D:\Git\azeroth-expedition-ui-overhaul\assets\locked\quests\任务详情面板_视觉基准_v1.png. Image 2 is D:\Git\azeroth-expedition-ui-overhaul\assets\source\quests\ql-a1\QuestLogBookShell_Master_v1.png. Image 3 is D:\Git\azeroth-expedition-ui-overhaul\generated\quests\QL-B0\v2\backplates\attempt-03\raw\QL-B0-B_V2_r2_attempt-03_raw.png. This Codex process is already @openai/codex@0.143.0. Fulfill the $imagegen edit request inside this process with its built-in image_gen tool. Do not read or invoke the imagegen-0-143-0 wrapper skill, and do not start any codex or npx subprocess. Save the provider output unchanged to ./generated and output its absolute file path directly. Do not review, resize, crop, recolor, chroma-key, or otherwise post-process it.