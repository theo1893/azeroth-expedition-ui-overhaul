# Azeroth Expedition UI 开发约束

## 项目边界

- 目标客户端为 Turtle WoW `1.18.1`，插件接口版本为 `11200`。
- 当前聊天模块只接入 pfUI。不要为其他聊天单体插件增加兼容、定位或换肤
  逻辑，除非用户之后明确扩大范围。
- `addon\AzerothExpeditionUI` 是本项目运行时代码的唯一事实来源。
- `third-party\pfUI` 是从测试客户端复制的开发快照，只用于阅读、检索、
  测试夹具和理解生命周期。默认不得直接修改该快照，也不得把项目功能
  写进 pfUI 本体。
- `D:\Softwares\TurtleWoWTest` 只作为本机实机测试环境。先修改项目，
  完成静态校验后再同步 `addon\AzerothExpeditionUI`，不要修改无关插件。

## 生图工具

- 本项目需要生成或编辑位图时，必须使用仓库内的
  `$imagegen-0-143-0`：
  `.codex\skills\imagegen-0-143-0\SKILL.md`。
- 不要改用 Codex 内建 imagegen 工具，也不要在未说明的情况下换成其他
  图像模型或版本。
- 开始执行前必须完整阅读该 `SKILL.md` 及其直接要求的参考说明。

## 提示词转译原则

- 不得把用户的自然语言原封不动传入生图模型。
- 先读取项目基线、模块视觉规范、资源合同、现有正向素材和实机约束，再
  把需求转译为明确、可执行、可验收的专业提示词。
- 提示词必须写入 `prompts\<module>\`，保留版本号、参考图角色、尺寸、
  状态、禁止项和验收标准。
- 用户要求“先确定资源”时，必须先列清物理资源、逻辑切片、运行时对象
  映射、画布尺寸、透明方式和状态数量，再执行生成。

### 聊天模块基线优先级

发生冲突时按以下顺序裁决：

1. `assets\locked\chat\聊天框视觉基准_v1.png`
2. `docs\modules\chat\聊天框视觉规范_战地旧书_v1.md`
3. `docs\艾泽拉斯远征手记_UI整合包提示词.md`
4. `assets\source\chat\` 内已经确认的正向母版
5. `assets\locked\chat\聊天框独立艺术资源_v3.png`
6. `assets\references\chat\` 内的实机截图，只用于比例和故障分析

被否决的版本只能作为反例，不能重新作为正向风格参考。

### 聊天模块锁定美术语言

- 第一眼必须是仓促修补、长期使用的旧战地日志，而不是规整复古面板。
- 纸张是信息主体，深棕旧皮革形成物件边界，氧化暗金只用于少量修补。
- 保留不对称装订、弯曲封皮、二至四层毛边书页、错层页角、非镜像修补
  和手绘轮廓。
- 禁止规则工业网格、网页 Tab、完整金属框、镜像四角、现代 HUD、
  黑红暗黑式框架、摄影级产品渲染和固定槽位。
- A/B/C 必须共用色板、磨损尺度、笔触、缝线尺度和光照方向。B、C 应以
  已确认的新 A 为最高材质参考。

## 参考图授权边界

- 用户已授权本次聊天模块工作使用项目内锁定基线、正向源素材、用户提供
  并已归档到 `assets\references\chat\session-2026-07-29\` 的实机截图，
  以及本项目生成的 A/B/C 候选作为 imagegen 参考输入。
- 该授权只覆盖本项目聊天模块，不自动扩展到其他模块、外部文件或第三方
  私有素材。
- 上传前在提示词中明确每张参考图的职责：风格、材质、比例、故障反例或
  主编辑目标；不得让模型自行混合互相冲突的结构。

## 生图执行与文件流

1. 读取并锁定基线与资源合同。
2. 在 `prompts\<module>\` 编写经过转译的专业提示词。
3. 使用 `$imagegen-0-143-0` 生成；原始结果只写入被 Git 忽略的
   `generated\`。
4. 若模型不能可靠输出真透明背景，要求使用完全均匀的
   `#00FF00` 色键背景。禁止假透明棋盘格、渐变、地面和投影。
5. 使用 imagegen 技能提供的色键去除脚本或等价的确定性流程转换为真
   Alpha；不得用第二轮自由重绘来掩盖背景错误。
6. 检查像素尺寸、RGBA Alpha 范围、边缘残色、透明孔洞、对象数量和
   各状态共同画布。
7. 同时检查 100% 像素视图与游戏内实际尺寸合成，不能只看放大图。
8. 用户确认后，才把最终透明母版复制到 `assets\source\`；锁定资源只有
   在用户明确确认后才能进入 `assets\locked\`。
9. 运行时资源必须由确定性脚本从母版导出，不得手工反复压缩。

## 聊天模块实际尺寸验收

- pfUI 最小聊天容器：`440 × 320 UI px`。
- 正文安全区：`x=30..410`、`y=44..280`，即 `380 × 236 UI px`。
- 容量目标：12px 字号、14px 行高时至少 16 行中文聊天内容。
- 四个 Tab 必须是独立状态资源，共用相同画布、底线和点击几何。
- Tab 状态切换只允许更换纹理或 UV；不得在周期维护中改变 Parent、
  Point、Width、Height 或点击框。
- 输入条与默认底栏共用底部预留，切换输入焦点不得让正文回流。
- pfUI 拖动期间不得重设聊天框几何；拖动完成后只做一次同步布局。

## Turtle WoW 运行时资源

- 使用 32 位 TGA、真 Alpha、2 的幂画布。
- 纹理 UV 周围保留防渗色边距。
- 九宫格只拉伸中央纸面、横边中段和竖边中段。
- Tab、输入条、底栏字段使用横向三段式；端帽、铆钉、缝线结和缺口
  不得横向拉伸。
- 运行时代码必须继续复用 pfUI 的聊天窗口、停靠、拖动、滚动、历史、
  输入框、底栏和 Tab 点击逻辑。

## 2026-07-29 暂停点

- 聊天 A/B/C V3 透明母版及 `440 × 320` 合成校验已经完成并归档。
- pfUI 运行时接入按用户要求暂停。
- `tools\build_chat_v3_runtime_assets.py` 是刚开始编写的确定性导出草稿，
  尚未执行，也未生成或安装 V3 TGA。
- `addon\AzerothExpeditionUI\Modules\Chat.lua` 仍是暂停前的 `0.3.1`
  适配逻辑。恢复工作时应先复核导出脚本与 Lua UV，再继续接入。
